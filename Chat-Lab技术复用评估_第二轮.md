# Chat-Lab 技术复用评估：第二轮

日期：2026-09-12。目标保持为个人微信进入企微与个微混合外部群，持续分析新消息并主动回复。当前只评估文本机器人 PoC。

## 1. 本轮结论与上一轮的调整

**推荐把“同一个个人微信账号的本地数据库读取＋Chat-Lab 分析能力＋独立桌面文本发送”作为本轮主候选。它有具体源码基础，仍需修正读取边界并完成实际混合群收发验证。**

这条设计将持续读取放在数据库侧，桌面只承担最后发送。接收和发送分别验证，因此不要求一个桌面工具同时解决所有消息读取问题。若当前客户端的数据库读取无法通过验证，再使用桌面同时收发作为备选。

与第一轮相比，新增判断如下：

| 第一轮判断 | 本轮补充或修正 |
|---|---|
| Chat-Lab 可复用采集、分析 | 已追踪具体链路：发现文件、候选密钥验证、DPAPI、数据库解析、游标、SQLite、规则与模型分析 |
| 首选桌面同时收发 | 新增并优先评估数据库读＋桌面发；读取端可复用更多 Chat-Lab 技术，但现有代码还不能直接启动作为机器人 |
| 附件排除 `@openim` | 该结论仍只针对附件；Chat-Lab 的个微解析器没有相同排除规则，合成的 `@openim` 会话可以映射，真实混合群结构仍待验证 |
| 缺少发送模块 | 仍成立；还要补“只处理新事件、生成对外回复、发送状态记录”，不能只在分析函数后加一次发送调用 |
| 基于源码和文档 | 增加选定离线单元测试、合成边界探针；它们仍不等同于实际账号和混合群测试 |

## 2. 参考版本与真实变化

参考仓库：[JJ704sd/Chat-Lab](https://github.com/JJ704sd/Chat-Lab)。本轮实际取得的提交为 [`67bb1f1b6a8f5a89ce4cf18f65eaf38688639ab2`](https://github.com/JJ704sd/Chat-Lab/commit/67bb1f1b6a8f5a89ce4cf18f65eaf38688639ab2)，上一轮为 `a42137b`。

两个版本间共有 47 个文件变化，主要新增空运报价工作台、PDF／群聊双链路演示、展示与审核流程，以及 Mac 路径发现和未就绪阻断。个微发现、取数、解析、游标与监测核心并未随着这些界面功能一起升级。[版本差异](https://github.com/JJ704sd/Chat-Lab/compare/a42137baaed3e05acbf3863a257f43e1cb68e114...67bb1f1b6a8f5a89ce4cf18f65eaf38688639ab2)

本轮重新核验的 Windows 微信版本为 **4.1.13.65**。没有执行真实取钥、查询个人聊天库、重启客户端、调用在线模型或发送消息。

## 3. 可以参考的具体技术手段

### 3.1 个人微信：本地数据库采集

源码路径是：

```text
发现账号和 message_N.db
  → 尝试恢复候选数据库密钥
  → HMAC 与 sqlite_master 验证
  → DPAPI 本地保存已验证密钥
  → SQLCipher 只读打开数据库
  → 解析 Msg_* 表、会话和发送者
  → 用游标导入 SQLite
  → 规则／语义分析
```

可复用的价值在于：客户端正常保存下来的消息有机会作为结构化输入，不必持续从可见窗口抓取文本；已有游标与存储代码可作为改造起点。

密钥路径要区分两种机制：静态读取进程中的候选对象，以及登录阶段的调试器硬件断点捕获。后者会介入运行中的客户端，某些入口会重启客户端。它们都不是腾讯提供的群机器人 API。源码实现了捕获与验证过程，也不代表当前版本已捕获成功。仓库 README 仍记录此前微信 `4.1.12.55` 的只读扫描未通过验证。[历史实测说明](https://github.com/JJ704sd/Chat-Lab/blob/67bb1f1b6a8f5a89ce4cf18f65eaf38688639ab2/README.md#L42)

DPAPI 保护保存到本机的密钥，不能自动解决客户端版本适配、消息完整性或机器人发送问题。

个人微信这条实现是使用 SQLCipher 只读连接。下一节企微本地模块的 AES-128 解密、WAL 重放和快照完整性验证，不能直接当作个微链路已有的同等证据。[个微链路审查与实验](./evidence/round2-wechat-ingest.md)

### 3.2 企业微信本地库：值得借鉴的快照与解析

`wecom-local` 的实现包括数据库及 WAL 快照、一致性检查、wxSQLite3 AES-128 解密、提交帧恢复、SQLite 完整性检查、Protobuf 文本和嵌套记录解析，再按账号、会话入库分析。

其一致性检查基于重复读取与 SQLite 结构验证；代码未计算 WAL 滚动 checksum，也不保证四个库处于同一时刻的原子快照。这些检查不能单独证明零漏读。

这些机制可以指导个微读取模块的完整性设计，但其加密格式、表结构与用户标识属于企业微信客户端。不能把模块名称改为“wechat”后直接复用。

如果实际选择“企微账号取数、个微账号发消息”，还需证明两个账号都在目标群内，并建立跨客户端群标识映射；这增加一个独立验证点，因此不作为第一条 PoC 路线。Mac 分支目前主动返回未就绪，不能作为已可用的采集替代。[企微本地与官方采集审查](./evidence/round2-wecom-ingest.md)

### 3.3 官方 WxJava 会话存档：另一条读取来源

Java 采集器有真实的拉取、解密、映射、持久化和 JSONL 导出逻辑。它依赖企业会话存档的配置及权限，取得的是该权限范围内的内容，不属于个人微信账号托管接口。

还需区分“采集器数据库已提交”和“Python 已消费导出文件”：现有顺序存在数据库／游标已提交、后续 JSONL 导出失败的窗口。若未来采用该路线，应从已保存的数据补导或使用可恢复的待导出记录。上述读取能力仍需另外配套原群发送通道。[官方采集证据](./evidence/round2-wecom-ingest.md)

### 3.4 AI 与业务分析：复用分析函数，新增回复任务

已有规则分类、物流字段提取、主体识别、问题与已有回复关联，以及真实模型 HTTP 调用封装。企微语义路径还限制同账号、同会话的历史上下文，并用原文核对部分结构化结果。

但“评估群成员已经发出的回复”“提取报价”“生成本地待审核候选”，都不是“机器人生成并向群发送回复”。新工作台的页面提醒与报价预览也不能证明微信发送链路存在。

PoC 可复用消息格式、存储及模型调用封装，新增一个很小的任务：输入当前群的新消息和必要上下文，输出 `reply` 或 `ignore`；`reply` 附带回复文本和所依据的消息标识。群目标与账号由程序绑定，不能由模型从消息正文中自行指定。[分析复用审查](./evidence/round2-analysis-reuse.md)

## 4. 已发现的读取边界

以下问题均针对冻结的参考版本。本轮没有修改 Chat-Lab 源码。

| 边界 | 观察到的行为 | 用于机器人前需要处理什么 |
|---|---|---|
| 多消息分库 | discovery 枚举 `message_N.db`，取数目标却只列 `message_0.db` 和联系人库 | 为实际分库完成取数、密钥与游标对应；用至少两个分库验证 |
| 同秒新增消息 | 游标使用时间和字符串消息 ID；已读 ID 9 后，同秒后到的 ID 10 可能被判为旧消息 | 按原生字段定义数值／顺序语义，并对迟到消息采用有界回看和去重；单纯缩短轮询不能修复 |
| 跨表迟到消息 | 同一秒内字典序较小表的消息后来出现，可能落在全局游标之前 | 区分表／分库水位；对迟到与回填声明覆盖范围 |
| 新出现 WAL | watch 的文件集合在启动时固定，初始不存在的 WAL 后来创建时可能没有变化事件 | 监听预期路径的创建／删除并重新发现文件；验证只变化 WAL 的场景 |
| 账号与分库命名空间 | 个微取数虽接收 `source_key`，传给解析器时仍固定为 `wechat`；相同表名和本地 ID 可能入库覆盖 | 事件键纳入账号、分库和原始消息标识，不能只用表名和本地 ID |
| 本人识别与群映射 | 本人 ID、账号名推导和 `Msg_*` 反查有固定假设；合成 `@openim` 映射不验证真实外部群结构 | 对照企微、个微两端，验证群、发送者、本人消息；识别不可靠时不触发自动回复 |

源码定位与合成结果集中在 [个微读取证据](./evidence/round2-wechat-ingest.md)，可复跑探针为 [verify_wechat_read_assumptions.py](./verification/verify_wechat_read_assumptions.py)。探针复现限制的成功，不表示机器人读取功能已经通过验收。

企微本地模块同样要注意水位漏掉迟到消息，以及 upsert／重放统计包含旧消息的问题。**“本次处理了 N 条”不能直接解释为“出现了 N 条待回复的新消息”。**

## 5. 两种主接法如何选择

| 项目 | A：同个微数据库读＋桌面发（本轮主候选） | B：桌面同时读和发（备选） |
|---|---|---|
| 输入 | 数据库解析后形成的新消息事件 | 可见窗口及监听回调形成的新消息事件 |
| 主要复用 | Chat-Lab 采集、解析、存储、分析 | Chat-Lab 消息格式与分析 |
| 先验门槛 | 当前版本可读、混合群可映射、游标不漏读 | 当前版本和混合群窗口可识别、滚动和切换不漏读 |
| 新增工作 | 修正上表边界，关联数据库群与发送窗口 | 窗口生命周期管理、文本事件去重与上下文补齐 |
| 共同缺口 | 原群发送、生成回复、发送状态与自消息排除 | 原群发送、生成回复、发送状态与自消息排除 |
| 选择条件 | 当前账号的取数门槛通过后采用 | A 的取数门槛未通过时单独验证；B 的成功不能声称 A 已成立 |

wxauto 可以作为桌面发送适配器的候选，其 `SendMsg` 文档说明了文本发送接口，但没有因此证明本机 `4.1.13.65` 的混合群已适配。当前免费版安装页仍只列到 `4.1.8.107`。本轮没有安装、购买或激活该工具。[发送方法](https://docs.wxauto.org/docs/class/Chat.html)、[兼容范围](https://docs.wxauto.org/docs/install.html)

## 6. 最小衔接方式

```mermaid
flowchart LR
    DB[同一个个微账号的本地消息库] --> R[读取并规范化新消息]
    R --> A[Chat-Lab 分析与新增回复任务]
    A --> Q[去重和待发送记录]
    Q --> S[独立桌面发送器]
    S --> G[原混合测试群]
```

这里只需三个接口，不必建设通用 Agent 平台：

| 接口 | 最小输入输出与行为 |
|---|---|
| `read_new_messages` | 绑定账号和测试群；输出事件键、原群标识、发送者、本人标记、时间、文本和读取水位。旧历史可用于上下文，但不自动变成发送任务。水位与事件落库成功后一起提交 |
| `decide_reply` | 输入同群新事件与少量已存上下文；输出 `ignore` 或有依据的回复文本。数据缺失或解析异常时返回不发送状态 |
| `send_text` | 输入程序绑定的目标群、文本及回复任务 ID；输出 `confirmed`、`failed` 或 `unknown`。窗口校验失败就停止；结果未知时不盲目重发 |

这些是本项目拟采用的接口，不是 Chat-Lab 已经实现的 API。桌面工具返回调用成功也不等同于对端收到。验收时必须由另一个客户端确认；在没有可靠回执的情况下不承诺严格的“恰好发送一次”。

现有轮询默认值也不能直接沿用来承诺及时回复：通用个微 watch 为 600 秒，企微本地为 60 秒，Java 存档为 45 秒且每批默认 10 条。实验时应测量：

```text
消息出现 → 被读取 → 分析结束 → 调用发送 → 另一客户端看到
```

缩短轮询只减少其中的等待时间，不会自动修复漏读，也不会减少全量重分析的耗时。[采集时序及处理语义](./evidence/round2-wecom-ingest.md)

## 7. 本轮验证与下一步

本轮完成了源代码版本比较、分工审查、选定离线测试及合成数据边界复现。具体测试名、结果和未覆盖条件见三份第二轮证据记录；没有把模拟数据、伪调试器或本机 DPAPI 往返测试作为真实微信取钥成功的证据。

| 验证项目 | 本轮结果 | 能支持的判断 |
|---|---|---|
| 个微选定原有离线单测 | 13 项通过 | 选定的解析、密钥保管、模拟候选与清理逻辑可运行 |
| 企微选定原有离线单测 | 11 项通过 | 选定的合成加解密、阻断、隔离与解析逻辑可运行 |
| 分析侧选定原有离线单测 | 24 项通过 | 选定的分类、上下文、引用和导入逻辑可运行 |
| 新增合成边界探针 | 5 项限制复现、1 项字符串映射观察；主代理独立复跑一致 | 读取缺口具有可重复依据；不能据此判定真实混合群通过 |
| 当前客户端数据库和群内收发 | 未执行 | 不作通过判断 |

实际混合群的验证顺序调整为：

1. **R0：数据库门槛。** 在指定测试账号和当前版本上，验证目标消息库可读、混合群唯一定位、企微与个微发送者和本人消息正确；未通过则 A 路线暂停，评估 B。
2. **R1：连续读取。** 企微、个微各发 10 条编号文本，不 @ 机器人；验证全部读取一次。补测同秒消息、跨分库、新 WAL、程序重启和旧数据重放。
3. **R2：固定文本发回。** 绑定同一个测试群，发送三条固定 ACK，由两端确认；此步骤可提前独立验证，避免取数完成后才发现无法发送。
4. **R3：AI 主动回复。** 使用第一轮合成的到仓时间变更例子，无需 @ 自动合并分析并回原群；闲聊不回复、自消息不循环、重复事件不重复发送。
5. **R4：30 分钟观察。** 记录耗时、断线恢复、窗口切换和消息缺口，只据结果判断这套账号／版本／群的文本 PoC。

这些实际步骤仍待执行。第二轮能够确认的是：**Chat-Lab 为数据库读取和分析提供了具体可复用机制，也暴露了需要先处理的边界；混合群机器人整体仍是有条件可行，尚无真实收发通过的证据。**
