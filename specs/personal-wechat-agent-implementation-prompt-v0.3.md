# R0 自主操作提示词 v0.3

以下提示词供操作人在后续执行任务中使用。业务群聊模型不使用本提示词。依据：[操作 Spec v0.3](personal-wechat-agent-poc-v0.3.md)。

## A. 完整实施与实机执行

```text
请在 D:\path\to\ZJ-AI-WXBot 按 specs/personal-wechat-agent-poc-v0.3.md 实施并执行一次有界 R0。先读该 spec、v0.2、poc/evidence/handoff-2026-09-15.md 和适用项目指令，核查当前代码与未提交改动。

我授权你在唯一测试账号 wxid_example_account_dir 内，自行取得打开本机加密数据库所必需的密钥材料、只读解密或打开 message_0.db 及必要同账号分库，以定位“示例混合群”并核实本人身份。允许 spec v0.3 限定的只读进程访问及审阅后受限运行取钥/解密源码；不要要求我先粘贴密钥。排除 wxid_example_excluded_dir 和其他账号。不得 Hook、注入、修改客户端或源库，不得获取无关凭据。

这项权限只用于本地操作助手，不能授予群聊业务模型。密钥在本地受控工具中处理，只返回引用和状态，不输出到模型上下文、终端、日志或 Git。解密副本限本 run 的 gitignored 隔离目录，使用后按 spec 清理。

先修复配置和门槛缺口：G-KEY 不要求已有密钥，G-OPEN/G-DISCOVERY 不要求尚未发现的 conversation_key/self_sender_key；G-INGEST 必须要求已验证绑定。新增开关默认关闭，低层入口也须校验；保留旧 env:VAR 兼容方式。完成相关离线反例测试后，自行生成 run_id，记录本授权，在本地配置开启本轮读取、定位和客户端密钥材料权限。全程 read_only，allow_live_send=false。

按 v0.3 执行：复核账号及进程归属 → 自行取得必要材料 → 验证只读开库及 WAL 一致性 → 元数据唯一定位 → 核实本人 → 建立目标群 ID/时间基线 → 输出脱敏证据并清理临时材料。禁止全历史正文扫描、历史导入、外部模型调用和发送消息。“示例本人”仅是候选显示名，不能猜测本人身份；映射不唯一则停止绑定并说明缺证。

已有授权不重复询问，不因旧 v0.2 禁令停下；技术前置缺失先在范围内修复。平台确实拒绝某动作时，记录具体原因并继续独立可完成的工作，不绕过平台控制。不要创建后台任务，不 commit/push，保留无关改动。

最终交付改动摘要、实际测试结果、脱敏 R0 运行记录、临时文件清理状态及剩余阻塞。区分取钥成功、开库成功、R0 通过和 R1—R4 未执行；不以合成测试或文件存在替代实机证据。完成本次 R0 即停止。
```

## B. 接续已有实现

```text
继续本项目按 specs/personal-wechat-agent-poc-v0.3.md 执行 R0；本次确认沿用其单账号、单群、只读自取钥和解密范围。先核对最近 run 记录、代码差异及实际配置，不复用已失效的进程归属、密钥或绑定证据。已验证且仍有效的步骤不重复执行；没有实现的新开关先补实现和测试。模型自行生成新的 run_id 并记录授权引用，不要求我重复提供密钥。执行到目标群与本人可靠绑定及元数据基线完成，或出现明确技术/平台阻塞；全程不发送、不读取历史正文、不调用外部业务模型。按 v0.3 交付真实证据与限制。
```

## C. 收尾交接

```text
请只基于本次实际执行结果出具 R0 交接：写明 run_id、适用 spec、代码与配置状态、账号范围、取钥方式类别和引用名（无密钥值）、开库与 WAL 一致性结果、群定位候选数、本人映射依据、基线、清理结果和未完成项。每项标注通过当前限定场景/部分通过且有限制/未通过/未执行。给下一任最小下一步，明确哪些授权已经存在、哪些技术能力仍未实现；不要将平台限制写成用户未授权，不要将本文档或离线通过写成实机通过。不得附原始库、密钥、内存转储或历史聊天正文。
```
