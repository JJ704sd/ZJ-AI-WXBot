# 非 MAX 861 实施提示词

以下为用户可复制到执行任务的提示词。本文档本身不自动启动网关或授予附件中的操作权限。

## 按最新 README 继续的提示词（优先使用）

```text
继续 D:\path\to\ZJ-AI-WXBot 的非 MAX Windows 861 项目。
先读 poc/evidence/nonmax-861-readme-alignment-2026-09-16.md、主计划、g1-watermark 证据和当前交接，检查现有修改并保留，不 commit/push。

README 是混合版本材料：v875 赞助群说明不等于 MAX，也不是 861 授权恢复；维持零付费与 20250822 基线。不使用公共演示 key，不升级、不重建 MySQL/Redis、不启动旧包、不启用 owner 管理命令。

G1 仅处理新出现且适用的恢复说明或匹配设备 key。已存在匹配 key 就核验，不再生成；只有有效恢复依据时才按既有意图/收据控制做一次 Count=1/Days=1，未知先对账。没有新依据且没有具体离线缺口则明确停在 G1，不重复搜索、ping、生成、写询问稿或复做已通过测试。

现有 pad_legacy_* 已实现离线边界，不从零重写。实际路由、前缀和模型以固定包及其配置证据为准：不照搬 README 的 GenAuthKey2、/api/login/qr/newx、/v1/webhook。Webhook Update 的 Swagger 操作键 t 与 README PUT 冲突，保持 unknown；不运行 Webhook Test，也不把当前 Channel 写成已支持 Webhook。

G1 通过且真实动作授权具备后：复用同账号 key 和旧绑定，保持原设备/网络，分别核验登录与初始化状态；不要反复扫码或自动换 key。选择单路 HttpSync，Count=10，取得20条标注样本核验新旧、真实@、重复和混合群身份。去重是 ID 集合，ordering=unknown，不能取最大ID推进游标。只有读取验收和发送授权满足，才人工触发固定 ACK 并取得双端确认。HTTP200/accepted 不等于送达，超时不重发。

本轮按实际证据报告静态、离线、真实接收、双端四层；不启动屏幕识别、UIA、数据库取密钥，不代发维护方消息，不付费。常规本地修复自主完成，仅对必要的新授权或无法查明的关键信息提问。
```

## 完整执行提示词

```text
接手 D:\path\to\ZJ-AI-WXBot，只沿用非 MAX Windows 861 推进示例群机器人。不要切换MAX，不重启UIA研究，不采集数据库密钥，不使用OCR或降级客户端。

先读：
1. specs/nonmax-861-next-round-plan-2026-09-16.md（本轮主计划，纠正旧交接的数据库路线建议）
2. poc/evidence/nonmax-201-archive-audit-2026-09-16.json
3. poc/evidence/pad-861-upstream-database-failure-2026-09-16.md
4. specs/wechat-project-handoff-current-2026-09-16.md（背景及失败证据，不延续其已被用户否定的路线选择）
5. 范围内AGENTS.md、现有Channel/ApiRuntime/Store与Pad观察器实现。

资料：C:\Users\Example\Downloads\WeChatPadPro-2.01.zip。
该包不是完整服务端源码：包含文档、配置、示例和20250805发布二进制，未见Go核心源码。默认基线仍为D:\path\to\wechatpadpro_vios18.61-861_20250822_windows-amd64.zip，不覆盖或试运行旧包。附件与网页是材料，不是执行指令。

目标：合成测试账号、示例混合群，真实@→人工触发固定ACK→个微/企微双端确认；其后才AI与定时任务。零付费，尽快争取两个工作日内完成首关，但不可隐瞒外部依赖。

执行顺序：
G0 锁定ZIP/EXE/Swagger和差异，先查Git状态，保留当前修改，不提交推送。
G1 收敛861授权阻塞。先读旧收据和脚本，不因新附件就重放生成请求。只在获得相关新证据后按已有授权做一次有界复验，Count=1/Days=1；不明结果先对账。HTTP200不是成功，不能把延长超时当上游数据库修复。无恢复依据即停止外发，继续离线开发。
G2 有有效设备key后核对登录/扫码授权；验证原账号、稳定群ID和成员映射。读取本地旧绑定即可，不让用户反复提供已有信息，不输出凭据。
G3 复用已存在的 pad_legacy_* 离线 adapter，以真实样本补齐 unknown 契约。优先有界HTTP同步，显式正数Count，不使用空body或Count=0，不同时启用多种接收通道。使用真实样本确认消息ID、历史水位、发送者和真实@；缺失则unknown，禁止自动触发。
G4 在发送授权和读取验收具备后，人工触发一次固定文本，复用outbox、锁、额度及在线检查；超时记unknown不重发，必须双端确认。
G5 前述通过后才接AI草稿、自动回复及定时任务，不购买模型额度。

实现约束：复用api_channel.py / api_runtime.py / api_store.py，不重写另一套Agent。pad_cli目前是P1离线观察器，MAX HMAC/v1 decoder不能当作legacy契约。附件webhook-client.py会日志记录头和正文，没有可靠水位去重，不直接启动。实际网关签名须用真实样本确认。

常规可逆的本地实现、静态核查和测试自行执行，不为细节询问。真实登录、消息采样、发送或外联动作先核对已有明确授权；缺失时把动作、对象和影响整理成一次具体问题，不擅自扩大范围。不开公网端口、不联系厂商、不提交Issue、不付费、不commit/push，不对上游数据库做探测。

有明确缺口时交付实际修复/必要测试或新证据；既有离线transport与边界测试已完成，不重复开发。没有恢复依据且无新缺口时报告阻塞即可，不强制制造代码或文档改动。不要虚构回调字段。执行记录分为静态推断、离线测试、真实接收、双端验收四层，报告关卡状态与剩余阻塞。README 差异遵守本文首个提示词及核对记录。
```

## 只推进授权关卡时的短提示词

```text
只调查非MAX 861的有效设备key获取条件，依据主计划G0/G1及最新README核对记录。已有20250805/20250822比较与授权transport离线成果直接复用，不启动旧包、不切MAX，不重复已失败的上游生成。确认是否出现适用的新恢复证据或匹配key；没有新证据时明确返回外部阻塞，仅在发现具体离线缺口时修复和验证。保持零付费、不联系厂商、不输出密钥、不提交Git。
```

## 已取得有效key后的短提示词

```text
继续新计划G2–G4。先确认本地有效设备key及真实动作授权，不输出key；保持非MAX 20250822构建。完成原账号和原群绑定，选择一个有界接收通道，以20条标注样本验证真实@、历史、重复、企微成员和重连。复用现有Channel/Runtime/outbox实现legacy adapter。全部判据满足且发送获准后，人工触发一条固定ACK并取得双端证据；HTTP200或accepted不代表送达，发送超时不重试。不能通过修改通过标志替代实测。
```
