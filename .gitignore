# Local credentials and account configuration
.env
.env.*
!.env.example
*.pem
*.key

# Chat data, exports and original attachments
*.db
*.db-*
*.sqlite
*.sqlite3
*.sqlite3-*
*.zip
*.zip.temp
decrypted/
exports/
runtime/
logs/

# Local Python environments and caches
.venv/
__pycache__/
*.pyc

# Read-only reference checkouts and local assessment runs
.research/

# PoC runtime data (events, approvals, local evidence). Never commit source DBs or keys.
.local/

# Test caches
.pytest_cache/

# Private evidence and source documents (keep local, never publish)
poc/evidence/r0-group-ui.png
specs/*.docx
