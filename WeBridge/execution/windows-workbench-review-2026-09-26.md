# Windows 工作台检查与交付记录

日期：2026-09-26。独立项目：`D:\ZJ-AI-WXBot\WeBridge`。

## 最新状态：可重复使用的工作台最小收发闭环

2026-09-26 本轮把固定文本的一次性验证扩展为工作台中的可重复纯文本发送，并已将目标扩展为当前数据库副本中已读取并选择的好友或群会话。适配当前已接入账号及 Windows 微信 4.1.15.13；接收仍通过校验后的本地数据库副本。文件传输助手发送回读，以及一名用户指定好友的发送、本机回读和对端回复接收闭环已验证。该好友的送达由用户确认，未取得服务器投递回执；群聊尚未实际发送。

### 已交付流程

1. 选择读取范围内的目标会话并点“连接发送”，工作台隐藏启动常驻桥，校验客户端及账号。服务端核实本次请求的目标存在于当前副本、已在读取范围及发送桥支持范围内；准备后冻结账号、目标和正文，确认时与冻结草稿核对。启动网页和查看状态不自动附加进程。
2. 在普通消息编辑器输入文本，核对目标和正文后确认发送。支持中文、换行和 emoji，上限 2,000 UTF-16 单元 / 8,000 UTF-8 字节。每个请求在调用原生入口前持久记录；相同请求不会再次提交。
3. 发送前记录副本消息基线，发送后自动查询新增的本人同文记录。只有一个符合来源、类型、时间窗和基线条件的候选时，显示“已发现新的本人消息”，然后可以编辑下一条。
4. “断开发送”和停止工作台会请求桥正常退出，保留主脚本后再 detach，清理自身配置与凭证。调试组件随微信正常退出释放。

### 先前阶段真实验收：文件传输助手

通过真实 Edge 页面操作 `http://127.0.0.1:8787`，全程未拦截或替换业务 API：连接一次，先发 `WeBridge收发验证A0926：中文与换行\n第一条🙂`（其中 `\n` 为实际换行），再在同一连接发 `WeBridge收发验证B0926：同一连接第二条文本。`。两次都经过页面预检与确认，随后自动进入 `local_record_observed`；消息列表从 3 条变为 4 条、再变为 5 条，两条记录具有不同的非零 `serverId`。

通过页面正常断开后，8789 无监听，生命周期记录显示 `keeperPinned=true`、`mainScriptPinned=true`、`hostDetached=true`。微信 PID 56136 继续响应，验收期间无新增 Weixin / Frida 崩溃事件。工作台数据库状态继续为 `snapshot_ready`。本机原始结果保存于忽略目录 `.runtime/windows-minimal-loop-20260926.json`，没有凭证内容。

文件传输助手阶段的必要自动验证：发送客户端 16 项、HTTP 集成 7 项、常驻桥 5 项及原生命周期 19 项，共 **47 项 Python 检查通过**；原生脚本 **5 项核心合成检查通过**；真实 Edge 配合合成 API 的界面流程 **11 项通过**，涵盖两条中文消息、确认、回读、断开和窄屏。随后完成上述两条真实微信验收，没有重复全量或新增无关场景。

后续动态目标扩展补充验证了内部会话 ID、原生接收者长字符串赋值、提交目标冻结和账号/进程绑定：常驻桥 **7 项核心检查通过**，原生脚本 **6 项核心合成检查通过**。这些结果验证代码路径，不构成向真实好友或群聊发送的验收。

### 先前阶段：动态目标扩展后的真实界面复验

使用真实微信及工作台界面向文件传输助手发送 `WeBridge界面收发验证0926-1736：真实微信文本。`，经确认提交后自动进入 `local_record_observed`，消息列表由 5 条变为 6 条并自动显示新增本人文本；副本中的新记录具有非零 `serverId`。发送桥状态为 `targetPolicy=selected_conversation`，工作台发布的可发送目标列表包含 3 个当前读取会话。该阶段只向文件传输助手实际发送，当时好友和群聊尚未执行发送验收。本机结果保存在忽略目录 `.runtime/windows-conversation-loop-20260926.json`。

本轮还修复了多标签页共用 `engine.selected` 时，一个标签页浏览其他会话会干扰另一标签页草稿核对的问题。发送接口按本次请求的目标核实当前账号、已知会话、读取范围和发送范围，并与服务端冻结草稿核对；其他标签页改变浏览选择不会替换草稿目标，也不再造成无关拒绝。相关 HTTP 集成 **9 项**、发送客户端 **18 项**合成检查通过。

同一数据源的新副本现在仅刷新消息和成员缓存，保留正在编辑的正文及发送确认；自动恢复会话不再改写全局浏览选择。界面合成流程 **14 项通过**，新增回归覆盖准备后副本更新且另一页面切换会话时，原目标、正文及确认仍有效。

该阶段最终版本重启后，工作台保持运行，发送桥为 `ready`，文件传输助手显示 6 条消息且验收正文仍可见，编辑框已清空并可继续使用。更新前的桥正常断开，生命周期记录的保留及 detach 均成功，微信继续响应。该阶段按最小闭环范围暂停开发，未提交或推送代码。

### 本轮追加验收：用户指定好友

按用户指定，在真实工作台中向一名好友确认提交 **1 条测试文字**。随后自动进入 `local_record_observed`，该会话消息数由 **1 条增至 2 条**，新增本人正文自动回显。数据库副本记录满足 `isSelfKnown=true`、`isSelf=true`、`type=1`、`decodeStatus=ok`，且具有非零 `serverId`。这验证了指定好友会话的真实发送提交和本机副本回读。

最初用户表示暂时无法安排对端回复，该阶段仅验收发送和本机回读。随后用户确认“已收到”，只读核验新数据库副本发现该会话由 **2 条增至 3 条**：新增一条对端 `type=1` 消息，`isSelfKnown=true`、`isSelf=false`、`decodeStatus=ok`，发送者匹配测试目标，时间晚于测试发送，正文包含预定回复标识。因此该好友的发送、本机回读及对端新回复接收闭环已验证。

随后使用真实浏览器只读核验：该会话显示 3 条消息，新增对端回复以独立消息气泡回显，正文与预定回复标识完全匹配。此核验没有新增发送，完成后关闭专用浏览器。私有证据已记录 `replyUiVerification` 及 `bidirectionalLoopVerified=true`。

私有证据还记录 `incomingReplyVerified=true`、`recipientDeliveryVerified=true`；后者来源明确为 `user_confirmation`（“已收到”），不表示获得了服务器投递回执或已读回执。群聊未实际发送。测试正文、好友标识、消息标识及原始验收结果保存在本机忽略目录 `.runtime/windows-friend-loop-20260926.json`，发送和回复界面截图分别保存在同目录 `windows-friend-workbench-20260926.png`、`windows-friend-reply-20260926.png`；公开记录不包含这些私有内容。

收尾时测试草稿已清空、专用验证浏览器已关闭，工作台及发送桥保持 `ready`，该好友会话保留在读取范围内。本轮无需追加代码修复。

### 兼容与边界

旧的固定测试脚本及其耗尽记录保留；新常驻桥使用独立的多请求持久记录。工作台草稿表结构不变，只扩展结果 JSON；旧草稿缺少基线时不会自动得到新观察状态。旧服务器回执 ID 精确核对路径继续工作。

`local_record_observed` 与 `local_record_confirmed` 分开：前者表示新增同文记录的观察，`serverAccepted=false`、`localRecordConfirmed=false`，不承诺精确请求归因、手机端送达或已读；其他设备同时发送相同文字仍可能影响归因。多个候选或未知提交不会用文字猜测为成功。原生工厂 GUID 仅用于审计，因为当前副本中没有对应字段。

好友和群聊纯文本发送已接入当前会话流程；一名用户指定好友的真实发送、本机回读和对端回复接收已验收。该好友的送达由用户确认，尚无服务器投递回执或已读回执；群聊实际发送仍未验收。附件、真实 @、自动回复与定时发送未列入本轮最小闭环。原生字符串赋值与引用释放的静态依据见 [原生文本适配记录](../evidence/windows-repeatable-text-2026-09-26/README.md)。

## 前次状态：当前账号文件传输助手的一次性收发实测

用户要求再次尝试修复后，已在 Windows 微信 4.1.15.13、当前账号和 `filehelper` 会话上完成一次真实发送与数据库副本回读。范围仅限这次固定文本的一次性测试；没有验证任意会话、任意文本或重复发送。

- **接收**：用户从手机发送 `WeBridge接收验证0926` 后，数据库副本出现完全匹配的 type 1 消息，解码正常；本人账号的另一设备同步消息在库内标为 `isSelf=true`。其 `serverId` 已记录在本机 `.runtime/hook-receive-baseline.json`。
- **发送**：修复后的工具先执行只构造验证，12 / 12 回读通过且退出后微信仍响应。随后一次性 Hook 桥绑定当前账号与微信 PID 56136。准备前 `filehelper` 中没有 `WB-HOOK-0926`；服务端绑定目标“文件传输助手”和完整正文后，只确认提交一次。桥的持久闩锁仅有 1 条尝试记录。
- **发送回读**：工作台从新数据库副本读到且只读到 1 条精确匹配的 `WB-HOOK-0926`，type 1、`decodeStatus=ok`、`isSelfKnown=true`、`isSelf=true`、发送者为当前本人账号，`serverId=6769327891408372729`；同会话消息数量由 2 增至 3，快照版本为 `snapshot-z9k2ol7w`。旧接收验证文字仍可读。微信发送后保持响应，近时段无新增 Weixin 崩溃事件。
- **证据边界**：Hook 接口返回 `submitted_unconfirmed`，没有带可关联的服务器接受回执；工作台因此不会自动把草稿标为 `local_record_confirmed`。上面的发送成功结论来自这次事先确认无同文消息、一次提交后出现唯一本人消息的独立数据库核对。没有取得手机端已送达 / 已读证明，不将 `serverId` 或本机记录当成收件端回执。
- **收尾状态**：一次性桥已停止，凭证及配置已清理，提交闩锁保留；工作台继续运行于 `http://127.0.0.1:8787`，模式 `database`、状态 `snapshot_ready`，Hook 状态 `bridge_not_configured`。微信仍正常响应。没有提交或推送代码。本轮按用户先前“最小闭环后暂停”的范围停在此处。

这证明当前账号在上述固定范围内能从本地副本接收，并经适配的 Windows Hook 调用发送且回读本人消息。要成为可重复使用的发送功能，还需把一次性固定桥扩展为受控的通用文本桥，并建立不依赖人工比对的发送回读关联；本轮不以这次 PoC 代替该能力。

## 前次状态：卸载崩溃规避修复完成，按用户要求暂停

本节保留再次尝试修复之前的状态；最新真实收发结果见上节。当时用户要求“完成修复就先暂停这轮目标”，工作限定为修复调试组件卸载导致的崩溃、完成回归并保存证据。

- **真实接收已验证**：用户通过手机向文件传输助手发送 `WeBridge接收验证0926`，工作台从新数据库副本读到完全匹配的 type 1 文本，解码成功；同一账号同步消息的 `isSelf` 为 true。既有消息由 1 条增加至 2 条。原始验证标识留在本机忽略目录 `.runtime/hook-receive-baseline.json`。
- **发送尚未验证**：Windows 4.1.15.13 的固定文件传输助手发送入口已经完成静态适配，原生参数构造曾通过 12 / 12 回读，但 `sendCalled=false`，没有执行发送调用。不得把构造成功、合成测试或本机提交状态作为收发闭环证据。
- **故障与规避修复**：此前构造工具退出时卸载 Frida agent，微信发生 `frida-agent.dll_unloaded / 0xC0000005` 崩溃。独立 Python 进程中，不调用微信函数也能复现。现在先保留一个无行为脚本，再加载工作脚本；退出前保留工作脚本及其引用，组件随客户端正常退出释放。保留失败则不主动卸载，并保留超时后晚到的附加会话。旧的直接附加 / 卸载检查入口已禁用。
- **验证范围**：完整 Python 回归 **197 / 197 通过**（30.801 秒，启用 `ResourceWarning` 错误检查），其中一次性 Hook 工具 **19 项**，包括 **6 项**生命周期回归。独立进程旧卸载路径两组各 3 / 3 崩溃；修正顺序 3 / 3 正常退出。修复尚未再次用于真实微信，Frida 内部具体缺陷尚未定位。详见 [隔离诊断与原始证据](../evidence/frida-detach-diagnosis-2026-09-26.md)。
- **暂停前状态**：微信已恢复，主进程 PID 56136 响应正常；工作台 `http://127.0.0.1:8787` 为 `database / snapshot_ready`。8789 发送桥未启动，状态为 `bridge_not_configured`。保留工作台供查看，未提交或推送代码。

后续只有在恢复本轮目标后，才继续验证修复在真实客户端的效果和单条 Hook 发送、数据库回读；当前没有完成最小收发闭环。

## 历史记录：数据库读取与受控发送阶段

本节保留 2026-09-26 较早阶段的验证记录，最新状态见上节。项目已经新增 Windows 默认 `database` 模式，使用经校验的本地数据库副本读取；`demo` 与 Linux Docker `live` 模式仍保留。**真实数据库读取已取得正向证据；真实发送尚未成功，不能宣称收发闭环完成。**

| 本阶段项目 | 已有证据与边界 |
| --- | --- |
| 当前账号与客户端 | Windows 微信 **4.1.15.13**；先通过进程持有的数据库文件核对当前账号，未把较早选中的非当前账号目录当作当前登录账号 |
| 授权密钥准备 | 用户明确授权后，独立可选工具对当前账号执行受限只读取钥；**4 / 4 业务库**的首页 HMAC 匹配通过。密钥仅写本地 `.secrets` 新文件，不写入报告、浏览器或日志；工作台自身不会自动扫描内存取钥 |
| 独立数据库副本 | **4 个业务数据库完整性检查通过**；复制、逐页解密、WAL 校验合并后才激活整批副本，源数据库不通过 SQLite 打开、写入或 checkpoint |
| 真实读取 | 已列出 **25 个会话**；读取文件传输助手现有 **1 条 type 47 消息**，`isSelf` 已核对。该记录证明当前副本中的既有消息可以读取，不代表已经完成新发送消息的接收回读 |
| WAL 实际兼容修复 | 实际客户端的 WAL reset / 复用布局触发原严格处理路径阻断；依据真实 SQLite reset 行为补修，相关快照 / WAL 回归 **17 项通过**。保留提交边界、校验与失败停止要求，没有通过忽略 WAL 宣布成功 |
| 自动更新 | 已实现每 5 秒检查业务库 / WAL 元数据，变化后异步创建并切换新副本；持久记录当前与上一代路径，跨重启保持已知副本数量上限。合成用例覆盖新消息更新和世代数量；未记录归属的旧目录不扫描删除。尚无长期运行验收结论 |
| 原生 UIA | 恢复并置前当前主窗口后重新结构检测仍只有 **3 个节点（Pane 2、Window 1）**，未公开可识别的会话编辑与发送控件；这不是单纯由最小化造成的旧结果 |
| 可视手动发送 | 已实现预览图点选输入框 / 发送按钮、确认原稿为空、一次填字、新图复核正文、一次提交的独立流程。绑定当前账号、PID、进程创建时间和窗口几何；截图仅存内存，尝试标记在输入前落盘，重复请求或重启不会自动重发 |
| 当前发送实测 | 首次可视草稿准备在 **`before_input / window_changed`** 阶段中止；仍在诊断。**未成功发送，不存在已送达或闭环证据**。`submitted_unconfirmed` 即使出现也只代表本机提交操作返回，不能冒充接收回执 |
| 自动化验证 | 本阶段完整 Python 自动回归由主任务执行，**155 / 155 通过**；其中新发送状态流程 **19 项合成回归通过**。UIA / 可视 PowerShell helper 已通过 Windows PowerShell 5.1 编译及合成结构 / 位图验证。合成通过不等于真实发送通过 |

本阶段独立复核了数据库服务切换、失败清理、自动更新、发送路由及能力报告。HTTP 发送入口使用服务端当前 `sourceRoot` / `account` 覆盖浏览器字段，在数据源锁内执行；缺失 CSRF 被拒绝，数据库常规定时 / 自动回复 / 旧发送队列仍被禁用。可视发送能力独立显示，不能将只读数据库 Adapter 的能力解释成已验证的自动发送接口。

复核发现并修复了跨重启的数据保留问题：此前上一代路径只在内存，合成 SQLite 复现实验中重启刷新使副本由 2 代增至 3、4 代。现配置持久记录 `previousSnapshotRoot`，恢复时验证它是管理目录的直接子目录、非链接且不同于当前代，再清理更旧的已知副本；新增跨重启回归通过。未删除或修改真实源库。

本节真实运行计数采用主任务提供的本阶段验收结果；本报告维护任务只做源码复核和临时合成保留策略实验，没有重复读取真实聊天正文、取钥或执行输入。后续仍需完成指定测试消息的单次发送和接收回读，以及长期自动更新、完整群身份 / 真实 @ 和附件场景验证。

## 早期阶段历史记录：基线与范围

以下内容保留早期演示及只读窗口预览阶段的发现、测试数量和当时限制，不作为上述数据库阶段的最新状态。

- 上游：[15813489353/WeBridge](http://120.79.129.209:10093/15813489353/WeBridge)。本次检查时 `main` / `HEAD` 为 `66170b1f28c3a85493c740061d5dd5c54fce8f68`。
- 初始开发分支为 `codex/windows-workbench`。确认个人仓库后，本目录已整理为外层 `ZJ-AI-WXBot` 仓库的普通源码目录，上游 Git 历史归档保留，见 [来源记录](../UPSTREAM.md)。未提交或推送；外层原有 README 与评审材料修改保留。
- 原项目网页功能已有群聊、提及、定时、固定回复和富媒体；真实接入固定于 Linux 微信 4.1.1.8 ARM64 实验容器，不能直接复用于 Windows 微信。
- 用户本轮明确目标为本台 Windows 微信。新增本机检测、UIA 和本地 OCR 的显式只读预览；没有将 Linux 桥接或演示结果标为 Windows 原生收发通过。继续改造前再次执行 `git ls-remote`，上游 HEAD / main 仍为上述基线。

## 检查发现与处理

| 发现 | 处理与证据 |
| --- | --- |
| `server.py` 无条件导入 `fcntl`，Windows 导入失败 | 使用 Windows `msvcrt` / Unix `fcntl` 进程锁；互斥与释放测试通过 |
| SQLite 上下文只提交、不关闭连接，Windows 删除临时数据库时报 WinError 32 | 连接明确 commit / rollback / close；回归启用 ResourceWarning 错误检查 |
| Windows 可能缺少 IANA 时区数据库 | Asia/Shanghai 不可用时采用固定 UTC+8，保持每日调度当前时区契约 |
| 仅有 Mac Shell 启动入口 | 增加 CMD + PowerShell 启停、Python / 端口 / 运行目录权限预检、进程归属核对 |
| 服务失联仍显示连接，切群清空草稿，提交期间可重复操作 | 状态失联同步更新、账号与群隔离的会话草稿、提交互斥与保留提交中的新编辑 |
| 退出后旧轮询结果可能恢复旧账号连接状态 | 可重复并发用例先失败；连接代次校验后通过 |
| 连接代次修复使后台账号轮询丢弃正在进行的手动群列表刷新 | 分离账号和群列表请求代次；修前失败、修后通过，并验证旧群查询不能覆盖新查询 |
| 实际运行能力与“模式支持能力”混淆 | `supportsLiveSend` 与当前 `canSend` / `liveSend` / `connected` 分开；未连接不标记可发送 |
| 不同 Windows 账户的私有运行目录不能互相使用 | 发现隔离测试账户创建的目录阻止当前用户启动；归档本次合成 QA 目录后重新启动成功，并增加权限预检及可操作错误提示，没有放宽 ACL |
| 原生预览离页后残留 DOM 文字，迟到响应会恢复已经清除的结果 | 离页、隐藏页面、换窗、清除时销毁文字并递增请求代次；失败后必须重新检测 |
| `GetWindowRect` 含不可见缩放边框，可能误拒绝最大化 / 贴边窗口 | 截图及遮挡判断改用 DWM 可见边界；不可获取时失败；纯几何测试覆盖贴边、多屏和一像素遮挡 |
| 切群加载期间能保存尚未读取的空白回复规则 | 群资料就绪前禁用提交；旧请求不能覆盖新选中群或规则 |
| 定时页统计混入手动 / 回复消息，无变化轮询销毁键盘焦点 | 只统计 schedule 来源，注明最近记录范围；停用任务不再显示下次执行，无变化不重建任务按钮 |

## 交付功能

- Windows 双击入口：[start-workbench.cmd](../start-workbench.cmd)，停止：[stop-workbench.cmd](../stop-workbench.cmd)。使用说明：[WINDOWS.md](../WINDOWS.md)。
- 工作台：群名称搜索、已读取群筛选、当前消息 / 发送者搜索、@ 我的筛选、最新消息跳转、成员选择、群资料。
- 操作：分群草稿保留、Ctrl+Enter、模拟发送、防重复提交、定时创建 / 停用、固定回复规则保存 / 关闭、断线禁用与恢复。
- 模式：离线合成演示独立数据库；保留原 Linux 桥接接口和富媒体；Windows 本机预览结果独立展示、不入演示群和发送队列。
- 本机：只检测 Weixin / WeChat 进程版本及窗口元数据；预览需要主动选择可见未最小化窗口。UIA 限制为 400 节点、12 层、200 条文本、40,000 字符和 12 秒超时；本地 OCR 使用系统组件，对所选无遮挡窗口执行一次采集并仅返回文字。完整限制、临时文件处理和窗口竞态见 [WINDOWS.md](../WINDOWS.md)。

## 与上游思路的对应

保留上游 `Adapter → Engine → Store → HTTP → 工作台` 主链路，继续复用账号 / 群隔离、读取范围、发送队列、每日任务、固定回复、附件权限和“结果未知不自动重发”的行为。Windows 的界面读取放在独立入口，不能将 OCR 推断的名称作为原有接口要求的真实标识。

| 原有接口所需信息 / 行为 | Linux 基线 | 本台 Windows 当前证据 |
| --- | --- | --- |
| auth、selfId、groupId、成员标识 | 来自原实验桥接 | 只能确认进程 / 窗口，不具有这些标识 |
| 消息 ID、发送者、真实 @、持续新消息 | 原 DB + WAL / 桥接路径 | UIA / OCR 可见文字不满足上述契约 |
| 发送、定时、固定回复及回执 | 原 Linux 内部接口 | 演示覆盖工作台操作；Windows 真实发送尚未实现 |
| 附件与账号 / 读取范围校验 | 原媒体模块 | 保留原模块及离线测试；未证明 Windows 附件接入 |

`container_bridge.py`、`media_host.py`、`media_container.py`、`rich_content.py`、`video_download.py`、`install_bridge.py` 六个文件与上游基线逐字比较一致（仅归一化换行）。直接取上游四个原测试文件，在当前模块上运行原有 33 项，全部通过；不是只比较新增测试数量。

## 验证记录

1. Python 自动回归：`python -W error::ResourceWarning -m unittest discover -s web_mvp -p 'test_*.py' -v`。初轮 51 项通过；加入 OCR、HTTP、并发群刷新和几何回归后，最终 **71 / 71 通过**（15.026 秒）；日志位于本地 `.runtime/unit-tests-final.log`。
2. 启动实测：Windows PowerShell 5.1；Python 3.11.2 与 3.13.9；默认演示、含空格运行目录、端口冲突拒绝、停止归属校验均已运行。`python -S`（不加载第三方包）也能启动演示。
3. 浏览器真实操作验收共 23 项断言通过：演示标记、群搜索、消息搜索、@ 筛选、分群草稿隔离、切群恢复、刷新恢复、发送清草稿、单条模拟发送；重复提交仅一个请求、发送期间保留新草稿、创建和停用计划、保存和关闭回复规则、断线禁止发送、清除连接标志、自动恢复；1366 / 1024 / 600 宽度无横向溢出、本机检测返回、无可读窗口时禁用预览。
4. 原生状态实测：本机微信版本 **4.1.13.65**，检测到进程与主窗口；主窗口最小化，`available=false`，页面提示恢复窗口并禁用预览。没有读取真实聊天正文，也没有真实发送。
5. JS 语法及 `git diff --check` 通过。原 PDF / 图片 / 视频预览代码保留，媒体访问和分段下载原有离线测试通过；本轮没有做真实附件验收。
6. 原生界面另有 **20 项合成浏览器回归**通过：换窗 / 离页 / 页面隐藏清除正文，迟到响应不回填，UIA / OCR 互斥，失败后重新检测，无窗口禁用以及无自动原生读取；1366 / 600 / 390 宽度无横向溢出、零 JS 错误。脚本：`web_mvp/diagnostics/check_windows_ui.js`。这些请求全部被测试路由拦截，未调用真实正文读取。
7. 系统 OCR 实际执行：`python web_mvp/diagnostics/check_windows_ocr.py` 用自绘文字与纯白图片替换采集入口，真实运行 Windows.Media.Ocr。文字识别为 `WE B RI D G E OCR 12345`（中文字库将英文分词），忽略空白后与标记一致；纯白图片明确返回 `ocr_no_text`，未误标成功。临时文件已移除。损坏合成 PNG 返回 `ocr_decode_failed`，实际子进程超时返回 `ocr_timeout`。此项不含真实窗口截图。
8. 更新后重启本机 8787 工作台，`mode=demo`、`ready=true`。实际 HTTP 检查确认 inspect / OCR 缺失 CSRF 均返回 403，无效窗口被拒绝且不返回像素。仅做真实窗口状态与结构检查；当时 `available=false`，结构检查返回 `window_changed`。未读取正文或截取真实窗口。
9. 对照上游流程的补充浏览器回归 **7 项通过**：加载群资料时不能覆盖规则、迟到请求不切换当前群 / 不覆盖规则、待确认数仅含定时任务、成功统计注明最近记录、停用任务无下次执行时间、轮询保留键盘焦点。脚本 `web_mvp/diagnostics/check_workbench_ui.js` 全部使用合成 API；日志 `.runtime/workbench-ui-regression.log`。
10. 最后重新打开实际 8787 入口：演示群资料正常加载，390 像素定时页的新统计文字无横向溢出，零 JS 错误、零原生读取请求；更新桌面与 [OCR 诊断截图](../output/playwright/windows-ocr-diagnostics.png)。日志 `.runtime/workbench-final-review.log`。

截图（本地验收产物，未加入 Git）：[桌面](../output/playwright/workbench-desktop.png)、[1366 宽](../output/playwright/workbench-1366.png)、[600 宽](../output/playwright/workbench-600.png)、[本机诊断](../output/playwright/windows-diagnostics.png)。浏览器验收脚本保留在 `.runtime/browser_acceptance_a.js`、`_b.js`、`_c.js`，仅用于本机演示数据。

## 早期阶段历史记录：当时尚未完成的原生接入验收

续轮实机复核（2026-09-26）：客户端已由上述历史检查中的 **4.1.13.65** 升级为 **4.1.15.13**。实际 HTTP 状态与独立进程 / 文件元数据一致：5 个进程均运行 `D:\Program Files\Tencent\Weixin\Weixin.exe`，x64（PE `0x8664`），腾讯数字签名状态 `Valid`；文件更新时间早于当前进程启动时间。主窗口当时仍最小化，`available=false` / `no_visible_window`。未恢复窗口、读取正文或截图。当前检测每次查询实际进程，并核验窗口前后身份；未发现需要因此次升级追加的已知修复，不能据此推断新版读取兼容。

该早期阶段的完成审计：Windows 启停、工作台交互、原功能离线回归、本机检测和合成 OCR 已有对应证据；当时真实 Windows 会话读取仍缺正向实测，账号 / 群 / 消息身份、持续接收、真实 @、原生发送和附件适配仍未完成。该阶段的 `server.py` 仅有 `demo` / `live`，后者依然使用 Linux Docker；`WindowsClient` 不是完整的上游 `Adapter`。现有 PoC 的一次受控桌面填入 / 提交代码也不能替代带会话身份与回执的通用发送器。

本机窗口恢复并选定允许读取的测试会话后，仍需实际运行只读预览。即使取得窗口文字，也不等于取得可验证的群 ID、消息 ID、发送者、真实 @ 或持续消息流。Windows 原生发送、完整群收发、原生自动回复和长期运行均未实现 / 未验收，不能据本次工作台演示宣称完成。

该早期阶段没有启动 Linux 微信容器、改变本机微信登录状态或发送真实消息。后续数据库与受控发送阶段的最新进展见本文顶端，仍不能将工作台演示、代码实现或合成测试计为真实发送验收。
