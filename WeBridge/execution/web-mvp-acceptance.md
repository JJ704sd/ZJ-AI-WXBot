# 本机网页 MVP 验收

日期：2026-09-21，Asia/Shanghai。入口：`http://127.0.0.1:8787`。

当前账号“柒焦”，收发验收群“中技AI cosplay”。页面允许选择当前账号的其他群，但本轮没有向其他群发送测试消息。

下表是首版收发验收；本页后半部分记录同日新增的附件、账号与群读取功能。

| 项目 | 结果与证据 |
|---|---|
| 登录状态 | 页面正确显示当前已登录账号；登录弹窗可查看连接状态。二维码/手机确认已接入上游登录 WebSocket；本轮未退出账号，因此没有重新验收完整扫码过程。 |
| 群与成员 | 读取 2 个群；原测试群 8 名成员，另一群切换后读取 20 名成员，再返回原测试群成功。 |
| 普通消息与被 @ | 页面正确区分 RECV-001、RECV-002 和本轮新消息；“@ 我的”筛选正常。新普通聊天、只 @ 其他人的消息没有触发自动回复。 |
| 手动发送 | 从页面填写 MVP-MANUAL-001 并选择陈佳豪发送；回执 baseRet=0，serverId=`2283012269261534901`，taskId=797，uiOperations=0。 |
| 每日定时 | 从页面创建 17:20 任务，MVP-DAILY-001 自动发送并 @ 陈佳豪；回执 baseRet=0，serverId=`6140013267755605894`。随后已停用该测试任务，保留成功记录。 |
| 被 @ 固定回复 | 用户在页面填写话术并开启规则；黄宏达在 17:19:51 直接 @ 柒焦，17:19:53.79 进入回复队列，约 2.8 秒为消息时间戳至排队的观测差值，不作为延迟保证。回复绑定触发者黄宏达，baseRet=0，serverId=`6939794814247037901`，taskId=821，uiOperations=0。 |
| 接收端确认 | 用户明确确认自动回复消息可见，且黄宏达收到真实 @ 提醒。 |
| 前端检查 | 实际浏览器验收群切换、消息筛选、手动发送、创建任务、规则保存、登录状态；最终页面无控制台 warning/error。 |
| 自动测试 | 15 项通过，包含调度/自动回复/去重/账号隔离/重启/取消，以及 Host、Origin、CSRF 和 HTTP 重复提交检查。JavaScript 与 shell 语法检查通过。 |

发送均通过自有回调取得匹配回执，回调调用与释放完成。接收用正常 SQLCipher 读取临时 DB + WAL 副本；没有强制合并原消息库，也没有用界面点击完成消息收发。

## 第二轮：附件、合并记录与读取范围

| 项目 | 结果与证据 |
|---|---|
| 真实扫码登录 | 发现客户端已退出后，从新版“账号 / 登录”发起强制二维码流程。用户扫码授权，页面显示“柒焦 已连接”，随后识别出 2 个群。没有通过网页输入密码。 |
| 退出入口 | 页面显示“退出当前账号”，调用现有客户端退出接口；自动测试验证退出时关闭自动回复、取消排队消息、更新离线状态。本轮未主动再次退出已恢复的账号，真实退出操作尚未单独端到端验收。 |
| 读取群与全选 | 页面原勾选 1 / 2；点击“全选”再保存后显示 2 个群正在读取。切换另一群显示 20 位成员与已启用读取，再返回测试群。新账号默认空范围；取消读取、账号变化和查询期间退出的隔离测试通过。 |
| 群资料 | 测试群名称、群主、群 ID、8 名成员及个人/企业微信类别均在弹窗显示。 |
| 真实图片 | 消息 localId=6 的 JPEG、localId=24 的 PNG 均显示；浏览器 naturalWidth/Height 分别为 1188×1280、282×853。通过固定版本账号对象的有界只读内存取密钥后解码 DAT，无注入、无全内存扫描、无聊天窗口点击。 |
| 真实文件 | localId=31 的 PDF 经硬链接索引定位并读取，原文件 MD5 校验匹配。浏览器原生 iframe 预览空白，改为本地 PDF.js；实际 3 页，已点击翻至第 2 页并完成 993×1404 canvas 渲染。可下载原文件。 |
| 合并转发记录 | localId=32 的“群聊”卡片展开 4 条文字记录，发送者、时间、正文均可见。实现嵌套记录和记录附件入口，嵌套媒体尚未用真实样本验收。 |
| 撤回 | localId=29 正确显示微信提供的撤回通知，替代原“非文本消息”。带目标编号的撤回覆盖旧消息/移除附件入口通过自动测试；当前真实样本没有目标编号。 |
| 视频 | 已实现视频元数据、缓存定位、播放器、HTTP Range 和仅封面时重试；实际 HTTP 测试验证 206 字节范围及取消读取后的拒绝访问。后续收到真实样本并发现只返回封面；已修复官方下载与解密，实际 6.6 秒 H.264 视频在网页完整播放通过，见 [视频预览修复](video-preview-fix.md)。 |
| 表情与未下载附件 | 当前自定义表情缓存未解码成功，页面显示原因与重试；原生通用附件下载尚未实现。现阶段只能展示客户端已下载且当前适配的缓存。 |
| 自动验证 | 附件首轮 28 项测试通过；视频修复后共 33 项通过，JavaScript 语法检查通过。测试不发送真实微信消息。 |

图片 PNG 解码后 SHA256：`e67a85a5dede64db1f32d0d3b35f4f6f87590696032e70362a2b765cc0669c4c`；PDF SHA256：`d80a78442ef487be7129c0cbe4ff21feb4a5d8a98c029fd49f827b88318e4efa`。原始 XML 中的下载凭证和密钥不进入网页 API，也不写入本报告。

## 交付时状态

- 网页后台运行，绑定 `127.0.0.1:8787`；Linux 测试容器保持当前账号登录。
- **用户开启的“中技AI cosplay”自动回复保留开启，最小回复间隔 30 秒。** 页面可随时关闭并保存。
- **每日测试任务已停用**，没有留下次日重复发送的测试计划。
- 当前 2 个可见群均已勾选读取，可在“选择读取群聊”调整；未给另一群开启自动回复或发送测试消息。
- 本轮完整实现与说明在 [`web_mvp/README.md`](../web_mvp/README.md)。

## 尚未验证

- 长时间连续运行、网络重连和大量群同时监测。
- 其他来源视频、记录内媒体、自定义表情解码、完整原生附件下载，以及网页退出按钮的真实端到端操作。
- Windows、Linux x86_64、其他微信版本；当前原生桥仅适配 Linux 微信 4.1.1.8 ARM64。
- 原生下行事件订阅；目前仍为本地消息数据库副本轮询。

当前结果是可操作、已跑通真实收发与定时/自动回复闭环的 MVP，不代表完整微信客户端功能或生产可用性验证。
