# M3 单次受控回复试验

## 结束状态

操作人确认“收到”后明确要求结束本次受控测试。测试已结束；不继续发送，不启动实时自动回复或定时询问。后续阶段待新任务启动。现有发送锁及原始证据仅保留在本地；本文件只提交脱敏说明。

## 结论

M3 生成、真实微信输入框填入及单次发送点击通过；本机出现完整绿色气泡，输入框清空。操作人针对对端收件问题回复“收到”，本次受控回复取得人工收件确认；不是自动回复接入完成。接收方具体客户端类型未说明，不宣称个微与企微双端验证。

## 继续验证结果（12:11）

- 新一次只读截图首次明确返回 `window_occluded`；激活微信后截图成功，前置检查返回 `PREFLIGHT_PASS`。这证明当前遮挡可通过恢复前台解除，但不能追溯证明上一轮 `unexpected_failure` 的具体原因，也不证明后台运行稳定。
- `resume.png` 再次人工核对群名及现有草稿；没有重新填入或调用 M3。
- 恢复前确认旧记录无 `submit.lock`、`pre-submit.png`、`after.png`，保存旧失败状态与恢复核对依据。
- 单次 submit 退出码 0，持久状态 `submitted_unverified`，独占 `submit.lock` 存在，禁止再次发送。
- `after.png` 可见目标测试群中右侧绿色气泡 `UIA-ACK-<脱敏运行标识> 收到`；输入框清空，左侧会话预览亦显示该回复。
- 已请求另一群成员确认完整回复。尚未取得对端证据；本机气泡不能替代接收方验证。

以下保留前次失败过程，供追溯。

- 使用此前真实读取的 `UIA-READ-002 测试消息二` 作为人工选定输入，不声称实时自动触发。
- M3 一次请求，1.5 秒，229 tokens，生成 `UIA-ACK-<脱敏运行标识> 收到`。
- 真实窗口截图人工确认目标测试群标题及空输入框；具体标题不写入公开版。
- 首次填入被 `foreground_mismatch` 阻止，没有键盘输入。
- 有界等待目标获得焦点后填入一次；`staged.png` 证明文字完整、发送按钮已可用。
- submit 调用失败于发送点击前：没有 `pre-submit.png`、`submit.lock` 或 `attempted` 标记。错误被旧版脱敏归为 `unexpected_failure`，不能据此断言具体根因；窗口遮挡及焦点变化是已观察到的独立问题。
- 未重发；输入框可能仍保留该草稿。恢复前必须重新检查窗口、群标题和草稿，不能再次盲目填入。

## 实现与验证

`controlled_send_trial.py` 将 fill/submit 分离，校验窗口绑定及前台、坐标边界，发送前写入持久化 attempted 标记及独占锁；有 attempted 标记后禁止重试。新增常见窗口失败原因的脱敏映射。

相关离线测试 2 项通过（使用工作区独立 basetemp）；默认临时目录曾在 pytest 清理阶段遭遇权限错误，改用工作区临时目录后退出码 0。测试不证明真实发送。

## 本地证据

忽略目录 `poc/.local/controlled-send-20260920/` 保存 reply.json、plan.json、before-final.png、send-state-focused/staged.png 及失败状态。截图含真实群聊，仅本地保存，未提交。

## 下一步

操作人已回复“收到”。下一步进入独立的实时触发验证。尚未证明原生 @、账号/群内部身份、后台持续读取、自动触发或定时询问；原始 run 不进入公开仓库。
