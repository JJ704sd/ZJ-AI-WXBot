# 混合群记录排查与试读（附件目录对照）

日期：2026-09-15。
**run_id：** `read-20260915-73532865`
**关联 R0：** `r0-20260915-aee47c69`
**授权：** 操作人要求排查并尝试读取「示例混合群」企微/个微混合群记录；范围仍为测试号 `wxid_example_account_dir`，`mode=read_only`，`allow_live_send=false`。未发送、未调用外部业务模型、未进入 R1 二十条验收。

判定用语：通过当前限定场景 / 部分通过且有限制 / 未通过 / 未执行。

不含密钥值、源库副本。解密副本已删除。不要把本 run 写成 R0/R1 全通过。

---

## 1. 一句话结论

截图中 `msg\attach` 下三个哈希文件夹 **不是**目标混合群。目标群是 `example_group@chatroom`，消息在测试号 `message_0.db` 的 `Msg_example_group_table_hash`。已提交页中 **5/5 条可读**（2 条系统 zstd + 3 条文字）。该群无附件目录。`biz_message_0.db` 无该群表。WAL 未合并，12:00 之后是否还有未 checkpoint 消息未知。

---

## 2. 截图附件目录归属（已验证）

`msg\attach\<md5(username)>`。三个文件夹均能映射到测试号 Name2Id，**无一**等于目标群 MD5 `example_group_table_hash`。

| 文件夹 | 修改时间 | 映射 username | 是否目标群 | 处置 |
|---|---|---|---|---|
| `d09427d413adc6abd84f91ba0c675360` | 11:06 | 另一 `@chatroom` | 否 | 未读正文 |
| `4ca6cdd556e2bf538c62c60c7c5ac35e` | 11:14 | 测试号通讯录中的另一个 `wxid_` | 否 | 未读正文；未打开排除号自己的数据目录 |
| `86b9be3180abe42a51304bb48ddd264c` | 11:55 | 另一个个微 `wxid_` | 否 | 未读正文 |

11:55 附件与入群系统消息同时段，但是私聊图片，不是该群媒体。

---

## 3. 目标混合群定位补证

R0 只在 `contact` 标签中匹配成员企业特征，故 0 命中。本次在 `contact.db` 的 `chat_room.ext_buffer` 看到 **5 个 `@openim` + 测试号 `wxid_example_account`**，共 6 人；群主为 `@openim`。入群系统消息点名的企业前缀与 UI 线索一致，因此该内部键就是「示例混合群」，不是凭显示名猜测。

`biz_message_0.db` / `bizchat.db` 已打开：无目标群消息表。混合群落在个微 `message_0.db` 的 `@chatroom` 表，成员用 `@openim`。

---

## 4. 试读结果（仅目标表）

| local_id | 时间 | 类型 | 发送方类别 | 解码 | 摘要 |
|---|---|---|---|---|---|
| 1 | 11:55:37 | 10000 系统 | 未知 | zstd | 扫码入群，并列出五名企微成员企业/姓名 |
| 2 | 11:55:37 | 10000 系统 | 未知 | zstd | 说明上述成员为企业微信用户 |
| 3 | 11:55:47 | 1 文本 | 本人 wxid | utf8 | 单字符探针 |
| 4 | 11:55:47 | 1 文本 | `@openim` | utf8 | 欢迎语 |
| 5 | 12:00:05 | 1 文本 | `@openim`（群主侧） | utf8 | 与 session.summary 一致 |

会话摘要「噫，混入了一个个微」与第 5 条一致。`decoded_ok_count=5`。未扫描其他会话正文。

---

## 5. 限制

1. WAL 未应用到快照（`message_0.db-wal` 仍约 408 KiB）。
2. 这不是 R1：没有 20 条编号合成文字验收。
3. `chatroom_member` 为 `room_id/member_id`，本次用 `chat_room.ext_buffer` 与系统消息核成员，未再走完整 ID 映射表。
4. 未把绑定写回 `live.toml`。

---

## 6. 清理

解密副本已删：`message_0.db`、`biz_message_0.db`、`contact.db`、`session.db`、`hardlink.db`、`media_0.db`、`message_resource.db`、`bizchat.db`。`decrypted_db_leftovers=[]`。
