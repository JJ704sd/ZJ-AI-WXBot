# 下一轮观测判定与执行条件

状态：方案，未运行。目标是区分 R4 的候选路径问题和观测覆盖问题，不是宣布 Hook 接收验收。

## 已知与仍未知

R4 已有真实入站的用户确认与正常结束记录，三处候选均未记录命中。断点类型被接受，不等于地址回读或相关线程覆盖已验证。磁盘静态调用关系不能替代这些运行时证据。修复异常退出码后，旧 R4 的正常零命中仍待解释。

B 的入口有启用位；已检查的注册调用路径会先置位。该条件只影响 B 的后续处理，不应倒推成旧 R4 入口零命中的原因。C 的具体注册目标已解析为状态处理，后续任务未知。

## 什么时候开始测试标记

先完成观测器自身的地址回读、线程覆盖证据和窗口/清理检查，再宣布开始发送。应明确区分窗口前已存在的线程和窗口中新建的线程；若相关覆盖无法得到证据，结果必须保留 coverage_unknown。自建进程中的成功不能自动替代目标进程的覆盖证明。

三个候选点的诊断草案仍为业务推送入口、B 入口、旧候选入口。它们尚不是已验证的同进程正向对照。若没有独立且可核验的对照，三个点全零仍不能区分没有执行和没有观测到；不应直接重复上轮后宣布排除了某个根因。

现有真实附加实验额度已执行完成，本文件不新增或消耗额度。执行范围落实且上述观测条件满足后，才安排来自另一成员账号的分条入站测试；本轮不要求用户提前发送。

## 结果判定表

| 观察 | 可以得出的结论 | 不能得出的结论 |
|---|---|---|
| 覆盖证据不完整，任何点零命中 | 本次未记录该点命中，覆盖未知 | 该函数没有执行、地址一定错误 |
| 推送入口命中，B 未命中 | 记录到推送入口执行 | 该推送属于目标消息；B 注册或线程覆盖一定正确 |
| B 入口命中，下游未确认 | B 至少进入过一次 | 启用位通过、正文被解析、命中属于测试标记 |
| 旧候选命中 | 原候选至少执行过一次 | 已识别目标群、真实 @ 或目标消息 |
| 清理失败、窗口截断或异常退出 | 本轮无效或不完整，保留故障证据 | 以零命中作为有效负例 |
| 清理与窗口正常且所有点零命中 | 完整记录了当前方案的零命中 | 未有独立覆盖和正向对照时宣布候选失效 |

事件时间接近只构成线索。真实收消息验收还需要来源、方向、目标群、标记对应关系以及独立客户端证据；未取得这些信息前，不创建猜测字段的 decoder 或启用自动回复。

## 本轮完成的核对

- 核对 B 入口门槛及一条注册直接调用路径，记录至分支报告。
- 修正 spec 中 C 具体目标“未解析”的过期表述。
- 从原始 R4 时间戳重算标记窗口开始到结果结束为 25.100834 秒，该值包含清理，不能称为精确有效断点覆盖时长。未重新运行 R4。
- 子 Agent 完成独立文档一致性审查；其另一项静态分析被平台安全检查拦截，没有作为证据。

参见 [分支核查](windows-hook-branch-review-2026-09-18.md) 与 [R4 原因复核](windows-hook-r4-cause-audit-2026-09-18.md)。
