# R0 目标群定位线索（操作人截图）

日期：2026-09-15。操作人提供企业微信外部群窗口截图。本次只记录 **UI 可见的显示名与成员特征**，作为后续 R0 受限定位线索。

**未打开个微消息库、未遍历会话正文、未填写内部 `conversation_key`、未取钥、未发送。** 群名不能单独当绑定键。

原始 UI 截图仅保留在本地，未纳入公开版；本文件的显示名、成员特征和水印均已脱敏，不能单独证明测试个微客户端已打开该会话。

## 绑定对象（UI）

| 项 | 值 | 来源 |
|---|---|---|
| 显示名 | `示例混合群` | 窗口标题 |
| 群类型 | 企业微信用户创建的外部群，含 4 位外部联系人 | 副标题 |
| 群主 | 示例群主 @示例企业B | 副标题 / 成员栏“群主” |
| 群成员数 | 6 | 右侧“群成员 · 6” |
| 测试个微账号 | `wxid_example_account_dir`（此前已指定） | 路径绑定；截图未显示 wxid |

## 成员特征（按截图右侧列表，不猜内部 ID）

| 企业/分类 | 显示名 | 备注 |
|---|---|---|
| 示例企业A | 示例成员A | 截图水印账号 |
| 示例企业A | 示例成员B | |
| 示例企业B | 示例群主 | 群主 |
| 示例企业B | 示例成员C | |
| 示例企业C | 示例群主 | 与示例企业B群主同名不同企业，定位时必须同时约束，禁止只匹配“示例群主” |
| 微信联系人 | 示例本人 | 个微侧最可能的测试身份；**待操作人确认**是否即 `wxid_example_account_dir` |

系统提示（11:55）表明：显示名 `示例本人` 的微信用户经扫描二维码加入该外部群。这是定位线索，不是 `self_sender_key`，也不是 `conversation_key`。

聊天正文（议价/demo 等）**不写入绑定配置**，也不作为历史导入对象。

## 已写入本地草稿的线索（仍未开库）

gitignored：`poc/.local/poc/live.toml`

- `group.display_name = "示例混合群"`
- `group.member_features` = 上表六条 `企业/显示名`
- `group.required_detail_tokens` = 显示名、群主企业约束、微信联系人显示名
- `group.conversation_key` **仍为空**

## 判定

| 检查 | 判定 |
|---|---|
| 目标群显示名 | 通过当前限定场景（仅 UI） |
| 必要成员特征 | 通过当前限定场景（仅 UI；同名“示例群主”已分企业记录） |
| 群在测试个微库中唯一定位 | 未执行 |
| 内部 `conversation_key` | 未执行（禁止用群名或 `@openim` 猜测） |
| 自身份 `self_sender_key` | 未执行（`示例本人` 仅为显示名） |
| G-READ / 开库 | 未执行 |

## 仍缺

1. 确认：本截图的群就是 PoC **唯一**目标群；`示例本人` 是否为测试个微在该群中的显示名
2. `authorized_key_ref`（`env:VAR`）
3. 显式 `allow_live_read = true` 且 `read_only` 之后，才允许在 **已绑定账号** 上按显示名+成员特征做受限定位（禁止全会话正文遍历；不唯一则停止）
4. 定位成功后再写入 `conversation_key` 与 `self_sender_key`
