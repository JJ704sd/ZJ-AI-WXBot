# T0—T2 读取与真实 @识别

日期：2026-09-15。授权：`spec-v0.3 + operator-prompt-A-2026-09-15 + spec-v0.4-T1`。本轮只执行提示词 A；未发送、未调用 M3、未读其他群、未改源库/WAL、未 commit/push。

判定用语仅使用：通过当前限定场景 / 部分通过且有限制 / 未通过 / 未执行。

**总体：部分通过且有限制。** T0 通过当前限定场景。T1 部分通过且有限制（WAL 当前世代无已提交帧；新消息 3 条，不是 20 条编号样本）。T2 合成通过；实机 T2-01 真@本人通过当前限定场景，其余实机反例未执行。不以 3 条自然流量冒充 T1 20/20，不以单条 @ 关闭全部 T2。

---

## 1. 实现版本与工作树

| 项 | 值 |
|---|---|
| 仓库 | `D:\path\to\ZJ-AI-WXBot` |
| HEAD | `8e164f5`（`feat: expose bounded read and mention checks`） |
| 本阶段未提交 | `cli.py` / `mention.py` / `page_cipher.py` / `t1_pipeline.py` / `wal_view.py`；未跟踪测试 `tests/test_t0_config.py` `test_wal_view.py` `test_chat_room.py` `test_mention.py`；本文 |
| 离线测试 | 全套 pytest **110 passed**，退出码 0 |
| 本地绑定 | gitignored `poc/.local/poc/live.toml`；`allow_live_send=false`；`mode=read_only` |
| 模型 | 仍为 `mock`；本阶段未改 credential、未打 MiniMax |

新增/使用的只读路径：`wal_view.py`、`snapshot.py`、`chat_room_codec.py`、`mention.py`、`t0_check.py`、`local_binding.py`、`t1_pipeline.py`。CLI：`t0-check`、`t1-read`、`t2-scan`（这三条不打开共享 Store，避免与合成命名空间冲突）。

命令（均相对于 `poc/`，解释器 `.\.venv\Scripts\python.exe`）：

```text
python -m wechat_agent_poc.cli --config config.example.toml t0-check
python -m wechat_agent_poc.cli --config .local/poc/live.toml t0-check
python -m wechat_agent_poc.cli --config .local/poc/live.toml t1-read
python -m wechat_agent_poc.cli --config .local/poc/live.toml t1-read --write-binding
python -m wechat_agent_poc.cli --config .local/poc/live.toml t2-scan --self-sender-key wxid_example_account --field atuserlist=wxid_example_account --text "@示例本人 现在几点"
```

`--write-binding` 只允许写 `.local` 下的 `conversation_key` / `self_sender_key` / `binding_version` / `run_id` / `authorization_ref`。实机绑定写入一次，后续有界重读不再覆盖。

---

## 2. T0 本地检查

判定：**通过当前限定场景。**

| 检查 | 结果 |
|---|---|
| 旧 mode | `offline` / `read_only` / `draft_only` / `manual_send` 保留 |
| 新 mode 别名 | `auto_reply_test` / `scheduled_mention_test` / `auto_send` 不是合法 mode |
| 样例配置 | `config.example.toml` 四个 `allow_*` 均在顶层且为 false |
| `[adapters]` 下 `allow_*` | 解析器忽略，不武装 live |
| 发送 | `allow_live_send=false`；`t1-read` 在 true 时直接拒绝 |
| 低层 | 无钥时 open/ingest 阻断；发送阻断；取钥仅走 G-KEY |
| 缺项 | 实机配置仍缺企微+个微接收核验人（不影响本阶段只读） |

实机 `t0-check` 与 `t1-read` 内嵌 T0 报告一致：`verdict=通过当前限定场景`，`live_switches_from_adapters_do_not_arm=true`。

---

## 3. T1 实时读取

### 3.1 实机 run

| run_id | 绑定写入 | 判定 | 说明 |
|---|---|---|---|
| `t1-20260915-349bb83d` | 否 | **未通过** | 当时 WAL「有帧但无一有效」直接 halt；之后改为跳过上一代 salt 不符帧 |
| `t1-20260915-c3b030e2` | 是 | **部分通过且有限制** | 首次完整有界读取；写入本地绑定 |
| `t1-20260915-b74e2ff8` | 否 | **部分通过且有限制** | mention 空 protobuf 修正后的复核；最新证据以此为准 |

本地记录：`poc/.local/poc/runs/<run_id>/t1-record.json`。解密副本均已删除。

绑定（仅本地，核验后写入）：

| 项 | 值 |
|---|---|
| 账号目录 | `wxid_example_account_dir` |
| `self_sender_key` | `wxid_example_account` |
| 群 | 示例混合群 / `example_group@chatroom` |
| 表 | `Msg_example_group_table_hash` |
| `binding_version` | `t1-20260915-c3b030e2`（写入后未再改） |

### 3.2 门槛

| 门槛 | 判定 | 证据 |
|---|---|---|
| G-KEY | 通过当前限定场景 | 取钥前 Restart Manager 归属；候选 1；排除交集 0；`live_client_readonly_memory`；引用名 `env:WXBOT_R0_DB_KEY`（无值落盘） |
| G-OPEN | 部分通过且有限制 | `message_0.db` integrity_check=ok，62 页；WAL 存在（约 408 KiB）但 **当前世代 committed_frames=0**；99 帧 salt 与头不符（上一代），已跳过、未 halt、未写源 WAL |
| G-DISCOVERY | 部分通过且有限制 | 群唯一、`bindable=true`；`chat_room.ext_buffer` 抽出 6 个成员键；配置里的「公司/姓名」特征仍 **0 命中**（不在该 buffer / contact 标签） |
| G-INGEST | 部分通过且有限制 | R0 水位 `local_id=5` 之后可见 **3** 条新事件（id 6/7/8），去重后 3，**不是 20**；`numbered_sample_count=0`；**没有任何样本被证明来自尚未 checkpoint 的当前 WAL** |

`ext_buffer` 成员键（定位用，不是 @触发）：

- `10000000000000002@openim`
- `10000000000000001@openim`
- `10000000000000003@openim`
- `10000000000000004@openim`
- `10000000000000005@openim`
- `wxid_example_account`

尚未把「示例企业B／示例成员C」等配置特征映射到上列 openim 键。这不阻止本群唯一绑定，但 **不能** 当作 T6 目标键已确认。

### 3.3 有界新消息（不是编号验收）

水位 5 之后 3 条，正文长度已记、正文未写入公开证据：

| native id | 本地时间 (UTC+8) | sender | is_self | mention_self | packed_info_data |
|---|---|---|---|---|---|
| 6 | 2026-09-15 16:27:12 | `10000000000000003@openim` | false | false | 6 字节 `081010025800`，无成员 ID |
| 7 | 2026-09-15 16:28:54 | `10000000000000001@openim` | false | false | 同上 |
| 8 | 2026-09-15 16:29:00 | `10000000000000001@openim` | false | false | 同上 |

这 3 条已在主库已提交页，不在当前 WAL 世代。不能用它们关闭「WAL 未 checkpoint 可见」判据。

T1 最小验收（企微 10 + 个微 10、去重恰 20、至少 1 条当前 WAL 可见）：**未执行 / 未满足。** 缺参与者编号输入；程序不代写采集行。

---

## 4. T2 真实 @识别

### 4.1 合成反例（先完成）

判定：**通过当前限定场景**（仅合成）。`t2-scan` 只输出识别 JSON，不发送。

| 反例 | 期望 | 测试 |
|---|---|---|
| 结构化仅本人 | mention_self=true | `atuserlist=<self>`；reader 走字段不走正文 |
| 无结构化字段、正文写 @昵称 | unknown，不回退昵称 | `mention_field_absent` |
| @所有人 | mention_all，false | `notify@all` |
| @其他人 / 混合 @本人+他人 | false | 本轮不触发 |
| 引用容器内 atuserlist | 不视为 true | `refermsg` |
| 无法解析的 blob | unknown | 非 protobuf/XML |
| 实机非@ 的 6 字节 packed_info | false，不是 unknown | `081010025800` → `structured_empty` |

### 4.2 实机字段与 T2-01

`packed_info_data` 对文字消息多为 6 字节 protobuf 标志（`08 10 10 02 58 00`），**不含**成员键，不能单独当 mention 列表。真实单独 @ 写在 **`source`**（msgsource XML，库内常为 zstd）。必须先按消息同样方式解压，再读顶层 `<atuserlist>`；`packed_info` 为空标志时继续看 `source`。禁止用正文「@示例本人」判定。

截图 16:27 「@示例本人 示例文本，收到请回复，over」对应 native id **6**，run `t1-20260915-f4bc78c1`：

| id | 发送方键（与 UI 对照） | mention_self | 依据 |
|---|---|---|---|
| 6 | `10000000000000003@openim`（示例成员C@示例企业B） | **true** | `source` 顶层 `<atuserlist>` 仅含 `wxid_example_account`；reason=`structured_self_only` |
| 7 | `10000000000000001@openim`（示例群主@示例企业B，「没收到」） | false | 无 atuserlist |
| 8 | 同上（图片） | false | 无 atuserlist |

T2-01（真@本人）**通过当前限定场景**。示例群主「没收到」符合本阶段未发送。其余 T2 反例与 T1 20 条仍缺。

对照线索（不是 T6 目标键验收）：欢迎语发送方同为 `10000000000000003@openim`；「混入了一个个微」发送方同为 `10000000000000001@openim`。示例企业C同名示例群主仍未排除映射。

### 4.3 续读 `t1-20260915-21b7ca13`（17:55 后样本）

库内总行 13；水位 5 之后 8 条。WAL 当前世代仍 0 已提交帧。20 个编号 token 已出现，但挤在 2 条 native 消息里，不是 20 条独立消息。

| id | 时间 | 角色 | 识别 | 样本 |
|---|---|---|---|---|
| 9 | 17:55:51 | 非本人 | mention_self=false | 一条消息内含 T1-W-01…10 |
| 10 | 17:56:02 | 测试号本人 | mention_self=false | 一条消息内含 T1-P-01…10 |
| 11 | 17:56:34 | 非本人 | false，无 atuserlist，正文有 @ | 视为 T2-04 伪文字，未当 true |
| 12 | 17:57:01 | 非本人 | **true**，atuserlist 仅本人 | 又一次 T2-01，不是 @其他人 |
| 13 | 17:57:17 | 测试号本人 | false（结构化 @了其他人） | T2-06 本人原生 @ |

T1 编号 token：企微 10/10、个微 10/10；独立 native id 不足 20。T2-01、T2-04、T2-06 已有实机证据。仍缺 T2-02 原生@其他人、T2-03 @所有人、T2-05 引用@。

---

## 5. 阶段汇总

| 阶段 | 判定 | API 调用 | 发送尝试/确认/不确定 | 错群/重复/自身份 |
|---|---|---|---|---|
| T0 | 通过当前限定场景 | 0 | 0/0/0 | 无 |
| T1 | 部分通过且有限制 | 0 | 0/0/0 | token 20/20 但仅 2 条 native；WAL 当前世代不可见 |
| T2 合成 | 通过当前限定场景 | 0 | 0/0/0 | 不适用 |
| T2 实机 @ | 部分通过且有限制 | 0 | 0/0/0 | T2-01/04/06 已核；缺 T2-02/03/05 |
| T3—T6 | 未执行（本任务禁止） | — | — | — |

清理：成功 T1 run 的解密 `message_0.db` / `session.db` / `contact.db` 均已删；`decrypted_db_leftovers=[]`。密钥未进聊天/Git。源 WAL 未 checkpoint、未删除。

端到端时延：本阶段无发送、无接收核验，**不得声称时效达标**。

---

## 6. 参与者样本清单（关闭 T1/T2 所缺）

不要把下面文本预置进采集结果。全部发到唯一测试群 **示例混合群**。发完后由操作人再跑一次 `t1-read`（不要 `--write-binding`）。模型不得代发、不得为凑 WAL 去 checkpoint。

### 6.1 T1：20 条编号非 @ 文字

先发这 20 条。不要 @任何人。

**企微侧 10 条**（由非测试号成员从企微客户端发；sender 必须是 `@openim`，`is_self=false`）：

| 编号 | 请原样发送的正文 |
|---|---|
| T1-W-01 | `T1-W-01 企微编号样本` |
| T1-W-02 | `T1-W-02 企微编号样本` |
| T1-W-03 | `T1-W-03 企微编号样本` |
| T1-W-04 | `T1-W-04 企微编号样本` |
| T1-W-05 | `T1-W-05 企微编号样本` |
| T1-W-06 | `T1-W-06 企微编号样本` |
| T1-W-07 | `T1-W-07 企微编号样本` |
| T1-W-08 | `T1-W-08 企微编号样本` |
| T1-W-09 | `T1-W-09 企微编号样本` |
| T1-W-10 | `T1-W-10 企微编号样本` |

**个微侧 10 条**（可由操作人用测试号手工发；必须被标 `is_self=true`，不触发回复）：

| 编号 | 请原样发送的正文 |
|---|---|
| T1-P-01 | `T1-P-01 个微编号样本` |
| T1-P-02 | `T1-P-02 个微编号样本` |
| T1-P-03 | `T1-P-03 个微编号样本` |
| T1-P-04 | `T1-P-04 个微编号样本` |
| T1-P-05 | `T1-P-05 个微编号样本` |
| T1-P-06 | `T1-P-06 个微编号样本` |
| T1-P-07 | `T1-P-07 个微编号样本` |
| T1-P-08 | `T1-P-08 个微编号样本` |
| T1-P-09 | `T1-P-09 个微编号样本` |
| T1-P-10 | `T1-P-10 个微编号样本` |

若希望满足「至少 1 条尚未 checkpoint 的 WAL 可见」：在客户端尚未把 WAL 写入主库时通知操作人立刻 `t1-read`。不要为制造该条件去改源库。

通过口径：去重后恰 20 条编号、企微/个微各 10、WAL 限制单独记录。现有 id 6/7/8 **不算** 这 20 条。

### 6.2 T2：真实 @ 正反例（T1 编号发完后再发）

由 **非测试号** 成员操作，除非该行写明「测试号本人」。间隔可随意（本阶段无冷却发送）。每条发一条，不要连在一条里。

| 编号 | 谁发 | 操作 | 期望 mention_self |
|---|---|---|---|
| T2-01 | 非本人 | 原生单独 @测试号，正文含非空问题，例如 `现在几点` | true |
| T2-02 | 非本人 | 原生 @群内另一成员（不要点测试号） | false |
| T2-03 | 非本人 | 原生 @所有人 | false（mention_all） |
| T2-04 | 非本人 | **不要用原生@**；纯文字输入 `@示例本人 请回答` | unknown 或 false（不得因昵称变 true） |
| T2-05 | 非本人 | 引用一条带 @ 的消息并回复，本身不原生 @测试号 | 不得为 true |
| T2-06 | 测试号本人 | 自己原生 @自己或任意人 | 可解析，但 is_self=true，应答路径不触发 |
| T2-07 | 任意 | 不要重发已入库消息当新样本；验收时同一 native id 只计一次 | 重放忽略 |
| T2-08 | — | **不要在其他群发**。跨群本任务禁止读取，绑定外会话计零摄入 | 不读、零触发 |

T2-01 是关闭实机 @ 的关键样本：需要 `packed_info_data`（或并列结构化列）里出现 **测试号成员键** `wxid_example_account`，而不是显示名「示例本人」。

---

## 7. 未做与下一步

- 未启动 T3（已有独立证据，本轮不重跑）、T4、T5、T6。
- 未把 general_qa 接入微信 runner。
- 未映射示例企业B两位目标的内部键。
- 缺接收核验人，故 T4 即使后续启动也先列缺项。

关闭本阶段判据仍缺：参与者按第 6 节发送样本后的新 `t1-read`。在此之前保持 **部分通过且有限制**，不要进入提示词 C。
