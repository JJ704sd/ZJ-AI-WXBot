# R0 v0.3 实机运行记录（归属恢复后重跑）

日期：2026-09-15。适用契约：[spec v0.3](../../specs/personal-wechat-agent-poc-v0.3.md)、[恢复方案](../../specs/r0-account-recovery-plan-2026-09-15.md)。
**run_id：** `r0-20260915-aee47c69`
**关联旧运行：** `r0-20260915-6126326b`（G-KEY 测试号无持有进程，未取钥）
**中断运行：** `r0-20260915-7d6bade6`（归属与取钥已过，开库因项目环境缺 cryptography 崩溃；无解密残留）
**授权引用：** `spec-v0.3 + operator-prompt-A-2026-09-15`
**接续：** recovery-prompt-B-2026-09-15

判定用语：通过当前限定场景 / 部分通过且有限制 / 未通过 / 未执行。

不含密钥值、内存转储、源库副本或聊天正文。不要把 `r0-run` 退出码 0 或离线测试写成 R0 全通过。

---

## 1. 一句话结论

本 run **R0 部分通过且有限制**。目标库由唯一 Weixin 候选持有且与排除号无交集后，取钥与只读开库成功；目标群按显示名唯一匹配并建立元数据基线。限制：WAL 未合并；六条成员企业特征在联系人标签中均未命中。R1—R4 未执行。未发送、未调用外部业务模型。

---

## 2. 代码、配置与测试

| 项 | 值 |
|---|---|
| HEAD | `a9d7415e44b585457d5c5d0a5c47b12974cd32ac`（工作树仍有未提交差异） |
| 归属查询修复 | 目标/排除查询失败记未知并阻断；排除号独立进程不自动失败。本 run 前已落实，未再改语义 |
| 本 run 代码补丁 | 缺 `cryptography` 改为 G-OPEN 可记录阻断；R0 未捕获异常不再无判定退出 |
| 项目环境 | 在 `poc/.venv`（CPython 3.12）安装 `cryptography==50.0.1`（清华镜像；未改系统 Python） |
| 配置 | `mode=read_only`；三个读取/定位/取钥开关 true；`allow_live_send=false` |
| 离线回归 | 安装后抽样 `test_page_decrypt` + `test_process_own` 通过。完整套件本 run 未重跑，不以旧 74 passed 代替 |

`r0-run` 实际行为：要求 `read_only`、拒绝发送、调用 `execute_r0`。退出码 0 仅表示记录无 `halt` 且判定为通过或部分通过。

---

## 3. 归属（取钥前重新证明，未复用旧 PID 证据）

查询时间约 2026-09-15T07:53:02Z。方法：Restart Manager 文件持有者 ∩ Weixin PID。

| 检查 | 结果 | 判定 |
|---|---|---|
| 目标路径 | `wxid_example_account_dir`，未越界 | 通过当前限定场景 |
| 目标/排除查询 | 均成功 | 通过当前限定场景 |
| 候选 | Weixin 交集恰好 1；镜像 `Weixin.exe` | 通过当前限定场景 |
| 排除交集 | 0（排除号本 run 无持有者） | 通过当前限定场景 |
| 旧运行对比 | `6126326b` 时测试号 holder=0、排除号 holder=1。本次重新查询，不沿用旧归属结论 | — |

未为清零计数结束其他进程。未打开排除号数据库。

---

## 4. 阶段判定

| 阶段 | 判定 | 说明 |
|---|---|---|
| G-KEY 取钥 | 通过当前限定场景 | `live_client_readonly_memory`；引用名 `env:WXBOT_R0_DB_KEY`（无值）；material_count=46（同进程多 CipherConfig，仅用于本账号文件盐/HMAC 匹配） |
| G-OPEN 开库 | 部分通过且有限制 | `message_0.db` 62 页全部解密，`integrity_check=ok`；`session.db`/`contact.db` 同样 ok。适配器 `page_cipher_sqlcipher4_layout/v0.3` |
| WAL | 部分通过且有限制 | WAL 407,912 未应用到快照；只使用主库已提交页。未 checkpoint/删除源 WAL |
| 群定位 | 部分通过且有限制 | 显示名「示例混合群」会话候选 **1**：`example_group@chatroom`。六条成员企业特征 **0 命中**，不能用成员清单独立核验该群 |
| 本人映射 | 通过当前限定场景 | 目录 wxid 前缀唯一命中 `wxid_example_account`；「示例本人」作为显示名线索与该键一致，**不是**凭昵称猜测填入 |
| 基线 | 通过当前限定场景 | 表 `Msg_<md5>`；5 行元数据；`max_local_id=5`；`bodies_read=false` |
| G-INGEST / R1—R4 | 未执行 | 本 run 无新消息读取任务；未发送 |

**R0 总体：部分通过且有限制。**

---

## 5. 清理

| 项 | 结果 |
|---|---|
| 本 run 解密副本 | 已删除 `message_0.db` / `session.db` / `contact.db` |
| `decrypted_db_leftovers` | `[]` |
| 进程内 env | 已清除 `WXBOT_R0_DB_KEY` |
| `7d6bade6` 残留 | 无 `.db`；保留崩溃 stderr 作依赖证据 |
| 原失败报告 | [r0-v03-live.md](./r0-v03-live.md) 未改写 |

---

## 6. 剩余阻塞

1. WAL 未合并：若需把未 checkpoint 的页算进基线，要另做一致性方案，不能改源库。
2. 成员企业特征未在 `contact` 标签中匹配：群绑定目前主要靠唯一显示名。R1 前应补聊天室成员元数据或由操作人核对该内部键是否确为「示例混合群」。
3. 未把 `conversation_key` / `self_sender_key` 写回共享 live 配置，避免本 run 自动打开 ingest。

下一任若继续 R1，须单独授权读取新消息；不要把本文件或退出码 0 当成 R0 全通过。
