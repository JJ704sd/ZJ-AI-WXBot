# 当前群标题绑定检查

本轮新增本地 `group.display_name` 与上游精确标题控件的比较。证据仅输出控件数量和匹配布尔值，不保存标题、配置内容或消息正文。重复标题控件不算匹配；即便标题匹配，账号与群稳定身份仍未验证，读消息和发送保持关闭。

实测日期：2026-09-18。原生前置检查返回 `target_minimized`，在字节写入前停止；本轮未执行临时写入，也未获取标题匹配结果。详见同名 JSON。上一轮三个语义控件的结构证据仍有效，但不能替代本轮身份绑定。

源码复核：上游 `get_current_wxid` 从资料目录提取标识，其注释明确提示多账号缓存问题；`open_my_profile` 使用坐标点击头像。这两条路径尚不能直接作为当前登录账号独立证明。

验证：50 项相关离线测试通过，覆盖标题缺失、不匹配、唯一匹配、重复控件、隐私输出，以及既有结构探测、回滚和 UIA 读取门禁。这些测试不代表实机绑定成功。

下一步：恢复显示微信主窗口并保留原目标群，再运行有界标题比较。随后仍须取得本轮账号与原群的独立绑定证据，才能进入只读消息验证。没有启用 Agent 或自动回复。
