# T4 ACK1：未执行（2026-09-15）

授权：`spec-v0.4-T4 + operator-prompt-C-2026-09-15`。不调用 M3、不做原生 @、不 commit/push。不把 T1 20/20 或 T2-03 记为通过。

## 判定

**T4 本地实现：部分通过且有限制。T4 实机 ACK1：未执行。**

发送尝试 0 / 本地提交 0 / 接收确认 0 / 不确定 0。预算未消费。

## 已完成（本地）

- 原子预算 3；ACK 正文 `POC-ACK-<run_id>-00N`
- `t4-send --index 1` 才允许尝试；2/3 需双端确认
- `weixin_ui` 观察当前选中会话标题后再打字；顶层标题「微信」不能当群名
- `ObservedDesktopSender` 仅在 `allow_live_send=true` 时武装 UI
- gitignored `poc/.local/poc/live-t4.toml`：`manual_send`、两名核验人；观察失败后已改回 `allow_live_send=false`
- pytest 全套通过（127 passed）

## 实机未发送的原因

| 检查 | 结果 |
|---|---|
| 顶层窗口 | 存在，名称「微信」，类名 `Qt51514QWindowIcon` |
| 选中会话 UIA | **不可用**。唯一子节点 `MMUIRenderSubWindowHW`，Control/Raw 两层均无会话名、SelectionItem=0 |
| 系统焦点 | 「申请示例企微账号」（企微），不是微信 |
| 绑定群 | 配置仍为 `example_group@chatroom` / 示例混合群 |

无法证明当前会话是「示例混合群」，按 spec 停在 `submit_stage=not_sent`。

## 结转限制（不升级）

- T1：编号 token 20/20 挤在 2 条 native，不是 20 个气泡
- T2-02 / T2-05：未闭合；T2-03：无 @所有人权限，未执行
- T4b 原生 @：未开始

## 下一步（只需一条）

桌面 UIA 不再作为主发送路径。后续发送改为个微 API 接管，方案见 [personal-wechat-agent-api-send-architecture-2026-09-15.md](../../specs/personal-wechat-agent-api-send-architecture-2026-09-15.md)。

若仍要试 4.1 SendKeys：请把微信前台切到 **示例混合群** 并明确授权「不核验群名也发送」。API 通道验收前，不把本地气泡记为 T4 通过。ACK 2/3 仍要等企微 + 测试个微都看到 ACK1。
