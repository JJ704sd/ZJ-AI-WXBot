# R5 自然活动实测结果

用户明确授权本轮一次后，已执行一次且正常分离。没有再次附加。

## 实测

- 本机4.1.13.65，执行前重新确认同一进程身份和固定模块哈希。
- 三个点为业务推送入口、B订阅入口和旧候选入口；实际地址回读匹配，硬件类型被GDB接受。
- 观察开始UTC 08:39:33.657096，自然活动窗口开始08:39:50.663531，最终结束08:40:15.766286，总计42.110秒。旧字段 marker_window 指此自然活动窗口，没有用户标记发送。
- 三个点均0条命中。初始与最终线程清单均126项；有1项不在初始集合中，观察到1次创建和1次退出通知。不能据此推断变化的是聊天处理线程或证明完整覆盖。
- 断点表清空、detach完成、GDB退出码0。独立收尾确认debugger_present=false、创建时间和模块基址未变、主窗口存在且有响应。
- 没有正文读取、密钥读取、DLL注入、程序发送消息或夹具采样代码迁移。

## 附加完成后的只读核查

目标主进程持有1条已建立TCP连接；另一个WeChatAppEx utility进程持有6条。其余已枚举相关进程当时计数为0。只统计元数据，没有抓包或保留远端地址。此快照在观察窗口后取得，不代表窗口内发生过数据收发，更不能把连接数较多的进程直接认定为聊天接收进程。

## 能确认与不能确认

新增了真实进程中的地址回读和线程清单证据；R4当时未保存这两项，本次结果不能追溯补写R4。候选路径仍未获得真实执行证据。

由于本轮没有受控入站事件，也没有每线程寄存器覆盖证据，零命中不能解释为路径未执行、候选失效或微信没有收到消息。原始结果的execution_status=completed仅表示观测流程完成。

下一项具有区分力的实验应为：在一次新明确范围内，以来自另一成员账号、已确认本机收到的入站事件，对照新的业务推送/B/旧候选三个点。先前R4受控入站观察的是旧三点组，R5新点组没有受控入站样本，二者不能拼接成一次有效负例。即使新实验仍零命中，也需保留覆盖未知。

本轮一次授权已执行完。不会因零命中自动再附加，也不再增加通用夹具测试。静态调查、R4/R5的样本差异和剩余缺证分开记录，避免把多轮“不确定”累加成已证否定。
