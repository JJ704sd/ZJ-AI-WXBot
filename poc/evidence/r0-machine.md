# R0 本机探测（不打开聊天库）

日期：2026-09-14。本文件只记录可公开的客户端文件版本与默认路径是否存在。没有列出 wxid、没有查询消息表、没有读取进程内存、没有取钥。

## 实测客户端文件版本

| 客户端 | 路径 | FileVersion | 进程是否在运行（仅任务列表名） |
|---|---|---|---|
| 个人微信 Weixin | `C:\Program Files\Tencent\Weixin\Weixin.exe` | `4.1.13.65` | 是 |
| 企业微信 WXWork | `C:\Program Files (x86)\WXWork\WXWork.exe` | `5.0.10.6025` | 是 |

与 2026-09-12 文档基线微信 `4.1.13.65` **一致**，本次是本机重新测量，不是沿用旧记录。企微 `5.0.10.6025` 仅为对照安装事实；当前 PoC 主路线仍是个微账号采集，不把企微本地库当输入。

## 默认数据根

| 路径 | 存在 |
|---|---|
| `%USERPROFILE%\Documents\xwechat_files` | 是 |
| `%USERPROFILE%\Documents\WeChat Files` | 否 |

存在不等于可读。未进入该目录列举账号，未打开 `message_N.db`。

## 路线 A 门槛

| 检查 | 判定 |
|---|---|
| 本机微信文件版本 | 通过当前限定场景（4.1.13.65） |
| 目标消息库实际可读 | 未执行（无授权 wxid / 密钥引用；禁止自动取钥） |
| 目标群唯一定位 | 未执行 |
| 企微/个微成员与本人身份 | 未执行 |
| R0 总体 | 部分通过且有限制 |

因此路线 A **暂停于绑定与授权密钥之前**，不是“已证明当前库不可读”。按 spec，路线 B 须在 A 的取数门槛失败后才独立验证，本次 **未启动** UI 同时读写或真发送。

## 仍缺的最小待填项

1. 测试个微 `account.wxid`
2. 目标外部群 `conversation_key` / 显示名 / 成员特征 / 绑定证据
3. 已授权只读密钥引用（若走 SQLCipher）；不要从其他项目搜凭据
4. 企微与个微接收核验人
5. 若要真实模型：endpoint、模型名、凭据来源
