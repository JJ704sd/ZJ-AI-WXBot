# G1 阻塞：维护方待确认问题与恢复后执行清单

日期：2026-09-16。本轮无新增维护方答复、无适用设备 key、无对应构建的修复公告。未启动网关、未 ping、未生成、未登录、未收发、未重查 GitHub Release/Issue、未改代码或复跑测试。

## 本轮结论

| 项 | 判定 |
|---|---|
| 新依据 | **无** |
| 适用性 | 不适用；没有可验证的恢复说明或同构建备用签发入口 |
| G1 | **未通过** |
| 进入 G2 | **不具备** |
| 路径 | C：停止 G1 网络动作 |

本地只读：意图文件仍在；网关生成 Code=300 / 上游 `code=1`、无设备 key；`device-key.private.json` 不存在；`license-input.json` 指向本构建但 `credential_kind=admin_key` 且值为空，不是可用设备 key。1238/8098 无监听。

已确认、不必再向维护方核实的本地事实：自设 ADMIN_KEY 是文档建议的管理值来源；匹配入口是 `POST /admin/GenAuthKey1`；请求曾到达上游；失败在自动注册管理员密钥、查找系统用户时的数据库 27017 超时。MAX TokenKey 与 README `GenAuthKey2` 不适用。

区分（均未在本轮重新测量）：

- 入口可达：历史 `/ping` 200，不能当库已恢复
- 授权服务正常：未证明
- 数据库恢复：无一手说明
- 有效设备 key：无

## 交给用户的维护方询问（不代发）

可复制。不要附带 `.env`、ADMIN_KEY、原始上游正文或完整日志。

> 构建：非 MAX Windows amd64 `ios18.61-861 / 20250822_153444`
> EXE SHA256：`5308323f32d4249f325a750b0c0faa91aba5b91f50593c88931d9829ffb203e9`
> 流程：自设 ADMIN_KEY → 本地 `POST /admin/GenAuthKey1`（Count=1，Days=1）→ 上游签发设备 key。
> 2026-09-16 一次生成请求已到达 `adminkeyservice.knowhub.cloud`，HTTP200，业务 `code=1`，无设备 key。错误大意：自动注册管理员密钥失败 / 查找系统用户失败 / 上游数据库 27017 连接超时。本地网关最终 Code=300、Data=null。已用意图文件禁止自动重试。
> 请确认：
> 1. 该构建的自设 ADMIN_KEY 自动注册与设备 key 签发是否仍受支持？
> 2. 上述数据库连接故障是否已修复？修复时间以何为准？
> 3. 同构建是否有备用签发入口（非 MAX、非 README 中未进 Swagger 的 GenAuthKey2）？
> 4. 零付费前提下是否有额度/有效期限制？
> 5. 超时后如何只读对账、避免重复生成？需要哪些脱敏关联字段（不要完整密钥）？
> 暂不考虑 MAX 后台、付费套餐或更换构建。

## 恢复后最短执行清单（有新依据或有效 key 再做）

**A. 已有匹配本构建的设备 key**

1. 确认来源与使用授权；不要为验证而再生成。
2. 用现有 `apply_pad_861_license.py` 本地写入（`credential_kind=device_key`，`build_id` 必须为本构建）；`vendor_authorization_verified` 保持 false 直到状态核验。
3. 最小只读状态核验（在线/设备列表），不扫码、不收发。通过后进入 G2。

**B. 仅有“库已修复/服务仍支持”的明确说明、没有现成 key**

1. 核对已有意图与收据，确认无未决生成。
2. 已有授权范围内 **只做一次** `GenAuthKey1`，Count=1、Days=1。
3. 成功标准：非空且适用的设备 key，不是 HTTP200。
4. 未知结果先对账，不自动重发；同类 `code=1` / 27017 超时立即停止外发。

**随后 G2–G4（G1 通过后）**

1. 用户扫码；核对本机旧绑定中的合成测试账号与「示例混合群」，不输出标识。
2. 只开 HttpSync，Count=10，先核实限量与消费语义。
3. 至少 20 条标注样本后再谈自动触发；缺字段保持 unknown。
4. 发送获准后人工一条固定 ACK；accepted/HTTP200 ≠ 送达；超时 unknown，不重试。

当前不要：重放生成、改 Count/Days、动本地 MySQL/Redis、切 MAX、UIA/屏幕识别、取库密钥、降级或启动旧包、代发本询问、commit/push。
