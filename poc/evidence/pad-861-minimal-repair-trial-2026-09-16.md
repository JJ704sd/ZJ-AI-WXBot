# 861 最小传输修复及下一轮执行结果

## 已完成修复

- 区分 `connect_or_response_headers`、`response_body`、`response_complete`，记录单调时钟耗时；读体失败保留已收到的上游状态。urllib的open阶段仍不能进一步区分TCP、TLS和等响应头。
- 授权POST等待参数15秒、健康GET仍4秒；保留默认TLS验证。固定EXE自身5秒超时未改变。参数不是总时限，不宣称提高后即可登录。
- 在回写网关前，将完整上游正文独占写入gitignored私密收据；网关先断开也会等待正在进行的请求结束。没有重发、伪造响应或替换签名。
- 仍只允许一次POST外发、拒绝网关重试；新轮次单独意图和收据，不覆盖旧未知结果。
- 对不允许路径的小型请求体作有界读取后返回403，避免未读正文导致TCP关闭丢失403。
- 底层异常仅记录类型和整数错误码，不保存可能含URL/凭据的异常文本。

TDD首轮实际失败：读体TimeoutError丢失已收到的HTTP200。修复后该回归通过。另验证迟到响应保存、错误信息脱敏、路径隔离与重复阻断。最终相关 **37项通过、退出0**；未重跑全部Agent测试。

## 真实执行

用户要求修复并推进下一轮后，执行一次 `scripts/test_pad_861_auth_transport.py --late-response-trial`。原有未知结果保留，不把此次看作前次未提交的重试。两个方向的网关Block全程保持，无扫码或消息调用。

| 观测 | 结果 |
|---|---|
| 第一健康请求 | HTTP200，response_complete，1.703秒 |
| 第二健康请求 | URLError，connect_or_response_headers，0.672秒 |
| 唯一生成上游调用 | URLError，connect_or_response_headers，0.562秒，没有取得响应头状态 |
| 本地GenAuthKey1 | HTTP200、Code300、Data=null |
| 清理 | 网关停止、1238/8098无监听、配置摘要未变、双向Block核验通过 |

本轮生成结果仍unknown，没有取得设备key或迟到正文。新增底层类型字段是在这次失败后补充，不能用于追溯该次URLError的具体底层原因。之后仅两次无凭据健康GET，均200，没有再生成。

这组结果说明相同路径的连接表现不稳定，但不足以区分本地代理、TLS链路或服务端原因；不能直接认定授权无效或购买可解决。原始脱敏报告为 `.local/pad-legacy-861/auth-late-response-result.json`。旧收据及日志均保留且不对外分享。

## 真机演示判据与当前阻塞

当前停在取得有效设备key之前，不能展示扫码或真实群回复。后续必须先解决授权请求连接稳定性并核对未知生成状态，再依次验收原测试号登录、目标混合群身份与真实@/新旧消息样本、指定文本发送及双端送达。不能用离线测试或健康200替代真机验收。本轮没有推进未满足条件的登录/收发，也没有重启UIA路线。
