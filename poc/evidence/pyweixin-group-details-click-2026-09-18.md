# 原群详情面板实测推进

2026-09-18：当前群标题唯一且与本地目标一致。采用固定上游 `open_chatinfo` 使用的 `click_input` 后，首次定位到唯一 `mmui::ChatRoomMemberInfoView`；面板结构遍历 15 节点，无预算截断。窗口及标题在读取前后稳定。

当前结果仍不通过身份绑定：3 项详情词和 6 项成员特征均未匹配。探针仅读取面板内 Text/Button 的标签，无滚动；结果可能受到加载时序或控件角色覆盖不足影响，不能据此断定打开了错误群或成员不存在。没有读取聊天消息正文或发送消息，账号验证仍未执行。

本轮先测试 Invoke 与最多两秒等待，均没有找到预期面板；改用上游点击方式才定位成功。此结果支持后续采用点击方式，不等于单独证明 Invoke 永远不可用。

临时字节恢复为原值并读回成功。点击关闭面板后，立即检查仍发现面板，因此回执 `panel_restored=false`，不能声称界面已恢复。已在代码加入关闭后的最多两秒等待，但该改动尚未再次实机验证。面板可能仍展开。

早先 resume 回执把“预期面板缺失”标成 panel_restored=true，这不足以证明界面恢复；后续代码已修正，历史回执保留。

下一步需要观察详情面板的加载状态与实际控件角色，完善有界只读采样后重新比对；不能将标题匹配或可打开详情升级为账号／原群身份验证。当前消息读取、模型与发送均未启用。
