# Windows 861 授权生成超时核对材料

状态：供人工审阅的脱敏草稿，未对外发送。不要附加原始网关日志、.env、设备响应或ADMIN_KEY。

您好，请协助核对 Windows amd64 `ios18.61-861 / 20250822_153444` 的授权服务兼容性及一次超时请求的结果。

EXE SHA256：`5308323f32d4249f325a750b0c0faa91aba5b91f50593c88931d9829ffb203e9`。用户仅下载ZIP，没有另行取得厂商密钥。本地ADMIN_KEY是自设随机管理值。

2026-09-16，本机发现此构建的授权客户端不使用HTTP_PROXY/HTTPS_PROXY。通过其已有ADMIN_KEY_SERVICE_URL配置，将程序原始请求转发到固定 `https://adminkeyservice.knowhub.cloud`，保留TLS验证及请求体，不修改签名、不跟随重定向。GET `/ping`多次返回200。

一次本地GenAuthKey1（Count=1、Days=1）触发POST `/api/v1/public/auth/generate`，转发调用设置timeout=4并遇到TimeoutError；没有阶段耗时，无法判断发生在连接、响应头或响应体阶段，也不能证明请求已经到达服务端。首次请求可能已经被处理。程序自动重试被本地一次性限制拦截，没有第二次上游生成。调用方最终Code300、Data=null，没有收到设备key。502及409均是本地转发层映射，不是厂商授权拒绝响应。

随后仅调用GetAllDevices，两次Code200、devices为空；虽健康正常，程序没有发起上游 `/api/v1/auth/devices` 查询，因此不能核对服务端是否已生成。没有微信登录、扫码或发送消息。

请确认：

1. 该固定构建目前是否仍受支持；自设ADMIN_KEY是否足以使用正常生成流程，还是需要其它已明确说明的配置？
2. 能否通过安全渠道核对这次生成是否成功，并恢复现有结果，避免重复生成？请先告知最小必要关联信息；不要要求公开完整密钥或原始日志。
3. 本地设备为空时，是否有受支持的只读核对入口？为何GetAllDevices没有发起上游查询？
4. 生成接口正常响应时延、推荐超时及幂等/超时对账方式是什么？

目前保留所有本地试验意图及收据，未删除后重试，也未伪造授权响应。

后续补充：阶段观测修复后，又有一次经明确授权的独立生成试验，在connect_or_response_headers阶段0.562秒报URLError，仍没有响应头状态。未覆盖前次未知收据。2026-09-16 16:53起无凭据健康对照：curl显式代理5/5成功、未指定代理5/5成功、实际Python转发客户端5/5成功（均200及pong）。未指定代理组实际连接198.18.0.21，不能视为独立公网直连。健康可用尚不能解释生成失败或完成上游对账。
