# T4 固定文字通道：本地准备（2026-09-15）

授权推进：操作人要求暂搁 T1/T2 剩余信息核验、尽快进入下一阶段。本文件启动 **T4a 本地实现**，**未发送**、未调用 M3、未做原生 @、未 commit/push。

不把 T1 20 条独立气泡或 T2-03 伪记为通过。T4 成功也不回写为读取验收通过。

## 1. 上一阶段结转（不升级）

| 项 | 判定 | 说明 |
|---|---|---|
| T0 | 通过当前限定场景 | 发送默认关闭 |
| T1 | 部分通过且有限制 | 编号 token 20/20 挤在 2 条 native；WAL 当前世代不可见 |
| T2-01/04/06 | 通过当前限定场景 | 真@本人、伪文字、本人@他人 |
| T2-02 | 未通过/未闭合 | 新样本仍点到测试号，不是只 @其他人 |
| T2-03 | **未执行** | 操作人明确：当前没有原生 @所有人权限；不编造该样本 |
| T2-05 | 未闭合 | 尚无带 refermsg 的引用@证据 |
| T3 | 独立完成 | 本阶段不调用 |

## 2. T4 本地实现

新增 `t4_pipeline.py`、`tests/test_t4.py`、CLI `t4-check` / `t4-send`。离线 pytest 含 T4 后全套通过。

| 契约 | 结果 |
|---|---|
| 预算 | 原子 3 次；不确定后 remaining=0，不补发 |
| ACK 正文 | `POC-ACK-<run_id>-001` … `-003`，不含模型输出 |
| 模型 | `model_called=0`；T4 拒绝非 mock 模型 |
| 原生 @ | `deferred_not_selected`，不自行选人 |
| `[adapters] allow_live_send` | 不武装 |
| `ObservedDesktopSender` | `allow_live_send=false` 时不打字；true 时走 `weixin_ui` |
| `t4-send` | 条件不足或选中会话无法核验时 `submit_stage=not_sent`，退出码 2 |

本机顶层可见窗口标题为「微信」「企业微信」，**群名不在顶层 HWND**。禁止用窗口标题搜索代替当前会话观测。

## 3. 实机发送：未执行（窗口不可观测）

`live.toml` 仍为 `read_only` / `allow_live_send=false`，未改该文件去开发送。已另写 gitignored `poc/.local/poc/live-t4.toml`（核验人 `local-operator-wecom` / `local-operator-wechat`，`window.observer=weixin_ui`）。观察后 **未提交 ACK**，并已把 `live-t4.toml` 的 `allow_live_send` 改回 `false`。

WeChat 4.1.13.65 顶层 HWND 为「微信」，唯一子控件为 `MMUIRenderSubWindowHW`；Control/Raw 视图均无会话名、无 SelectionItem。当时系统焦点在企微窗口「申请示例企微账号」。按 fail-closed **禁止 SendKeys**。详见 `t4-ack1-2026-09-15.md`。

判定：**T4 本地契约部分通过且有限制；T4 实机 ACK1 未执行。** 发送尝试 0 / 确认 0 / 不确定 0。
