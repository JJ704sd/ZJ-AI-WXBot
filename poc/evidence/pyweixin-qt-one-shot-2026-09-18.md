# Qt 可访问性状态：一次实机修改与恢复

日期：2026-09-18。用户在前一次写入前停止后要求继续，沿用已确认的一次限定实验。仅当前账号客户端，不换账号／群、不登录、不发送、不读聊天正文。

## 结果

**已验证本机固定版本的候选字节会影响 UIA 根结构。** 原值 0；一次写入 1 并读回成功；观测后恢复 0 并读回成功。恢复前后均在原进程、原主窗口和固定模块的约束内执行。

| 阶段 | UIA 根类 | 已遍历节点 | 完整性 | 窗口绑定 |
|---|---|---:|---|---|
| 修改前 | Qt51514QWindowIcon | 2 | complete=true | 稳定 |
| 临时置 1 | mmui::MainWindow | 38 | complete=false | 稳定 |
| 恢复 0 后 | Qt51514QWindowIcon | 2 | complete=true | 稳定 |

窗口数均为 1，同路径微信进程数均为 6，版本 4.1.13.65。临时状态下会话列表、消息列表、输入框标志均未命中；由于遍历不完整，必须表述为 **unknown／尚未观察到**，不能据此认定这些控件不存在。当前探针有 128 节点、6 层及每次子节点数量限制；此次未保存细分截断原因，因此不能仅凭 38 节点断言具体在哪个控件截断。

这次 0→1→0 与根结构变化的对应关系，为候选开关提供了一次实机因果证据。结论仅限本次客户端状态；未验证跨重启、跨账号、长期运行或其他版本。没有追加第二次激活，没有将状态保持为 1。

## 原始证据与范围

- [原始收据](pyweixin-qt-one-shot-2026-09-18.json)，观测日期为 2026-09-18；精确时间已脱敏。
- 目标模块摘要与地址已脱敏。
- trial.write_attempted=true、write_readback=true、restored=true；收据 status=observed 表示完成观察，**不是聊天接入验收通过**。
- 命令完成用时约 1.71 秒，包含整个脚本流程，不能当作字节保持 1 的精确时长。
- 上一轮 44 项相关离线测试已通过；本轮未修改执行代码，未将历史测试数冒充新回归。

实际命令（工作区根）：

```powershell
<private-python> -X utf8 ZJ-AI-WXBot/poc/scripts/qt_accessibility_preflight.py --allow-temporary-byte-write --output research/pywechat-20260917/qt-one-shot-visible-trial-20260918.json
```

这是执行记录，不是后台任务或后续自动重试配置。

## 能力状态更新

- 当前账号客户端 UIA 根节点临时恢复：本轮已验证。
- 完整聊天语义树及消息列表／输入框：仍未验证。
- 当前账号和原群的独立身份绑定：仍未验证，根窗口不是群绑定证据。
- 新消息、发送者、原生 @、真实发送和双端送达：仍未验证。
- Agent、自动回复、主动消息：未启用。

原字节读回为 0，并观察到根结构恢复原状；这不证明所有 Qt 内部缓存或客户端风控状态均恢复，也不能推断没有延迟风控。当前实验没有采集账号限制状态或持续稳定性指标。

## 后续构建依据

下一项技术问题已从“是否能恢复根结构”变为“临时状态下完整结构如何分布，以及如何可靠绑定原群”。应先离线完善探针：分别记录节点上限、深度上限和子项截断原因；增加仅结构类名／角色／固定选择器布尔命中，不采集消息正文或成员名称。下一次独立实机实验再有界扩展深度，不能直接启动 Monitor／AutoReply 或把本次授权变为常驻激活。

本轮仅补充收据、说明和入口状态，未提交、未推送。
