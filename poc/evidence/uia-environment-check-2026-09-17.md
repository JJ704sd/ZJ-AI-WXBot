# UIA运行环境排除检查

用户要求继续尝试。本轮仅查询目标主进程模块名称/文件版本及进程令牌元数据，没有UIA激活、截图、消息读取、进程内存内容读取或权限修改。

## 结果

| 检查 | 实际结果 |
|---|---|
| 主进程 | 唯一有主窗口的Weixin进程 |
| 客户端 | Weixin.exe及Weixin.dll均为4.1.13.65 |
| MSAA运行库 | 已加载Oleacc.dll，7.2.19041.3636 |
| UIA运行库 | 已加载UIAutomationCore.dll，7.2.19041.4522 |
| 桌面会话 | 诊断进程与Weixin同Session |
| 完整性级别 | 两者TokenIntegrityLevel RID均12288（高） |
| 位数 | 两者WOW64=False；诊断主机64位，两者均非WOW64进程 |

这些证据不支持“低权限探针读取高权限微信”“32/64位不一致”或“微信未加载Windows无障碍运行库”的解释。模块已加载不证明腾讯提供了有效控件树，也不证明内部可访问性功能已开启。

## 后续处置

不进行无依据的系统DLL替换、组件重装或权限提升；没有新的源码/运行证据前，不继续相同构造器、选择器、讲述人和对象请求试验。当前已授权的纯UIA路线仍未打通，无法把外壳转换为不存在的聊天控件。

真正恢复需要客户端提供语义provider，或有能支持本版客户端的、可审查且不违反既定范围的访问机制；目前没有验证这样的修复。没有自动扩大到降级、进程补丁、数据库、OCR或发送，也未commit/push。
