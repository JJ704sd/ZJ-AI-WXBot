# R4：纠正样本方向后的三点采集

2026-09-18。当前可确认：**一次附加、三个点零命中、断点清除与正常分离完成。用户已确认三条有效入站样本在窗口内出现；当前三点配置未捕获命中，原因尚不能定位到候选路径或调试覆盖。**

## 授权与前置核对

用户对新增一次实验明确回复“可以”：最多 90 秒、三个硬件断点合计最多 10 次命中、最多 3 条独立人工标记，不读取正文或密钥、不注入 DLL、不主动发送。

首次预检未找到可见主窗口，没有附加。用户随后打开主窗口并明确确认账号和群与前日相同；重新识别到唯一主窗口，版本仍为 4.1.13.65，Responding=true，无 GDB 残留。运行器核对当前进程路径、启动时间以及目标模块磁盘哈希；未复用前日 PID。首次预检不计为附加。

## 实际执行结果

|项目|结果|
|---|---|
|附加次数|1|
|观察点|外层入口、分支汇合点、下游入口|
|硬件断点类型|三个插入结果均已核对|
|命中数|各 0 次|
|脚本开始|已脱敏|
|标记窗口开始|已脱敏|
|标记窗口长度|约 25 秒|
|脚本结束|已脱敏|
|总耗时|约 45 秒，包含设置/等待/清理，不是线程暂停时长|
|清理|断点表为空，分离成功，GDB 退出码 0|
|退出后|同一启动时间，Responding=true，GDB 进程数 0|

见[原始统计的脱敏收据](windows-hook-inbound-r4-2026-09-18.json)。运行器路径和脚本摘要不公开。此轮只增加时间记录并使用新运行目录，观察范围和限额没有扩大。

用户收到明确发送提示：由**另一个群成员账号**向原群分三条发送独立人工标记，间隔约 5 秒；本机应显示为左侧接收方向。提示后才开启标记窗口；结束后明确要求不要补发或重发。

## 样本核对与结论边界

样本状态：**用户已明确确认**三条消息由另一个群成员账号分三条发送，并在窗口内于本机显示为左侧收到的消息。该确认覆盖账号方向、消息粒度和窗口；本轮未取得独立截图或原生消息事件，证据级别仍是用户确认，不冒充自动核验。

零命中是本次运行结果。结合用户对样本方向和窗口的确认，可以判定当前观测配置未捕获这批入站活动，但不能独自区分候选路径错误、处理进程不同或实际线程断点覆盖不足。GDB 接受断点及夹具通过不等于所有真实线程的调试状态已独立验证。

本次授权的一次附加已执行完。当前不追加第五次附加、不提高限额、不读取全局阈值或消息对象、不改变接收/发送开关。样本条件已确认，下一工作包改为区分观测链路与真实接收路径，不重复同一配置的人工消息实验。
