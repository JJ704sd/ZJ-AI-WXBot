# 一次性 Qt 状态实验：写入前停止

2026-09-18，用户在说明临时状态实验及恢复限制后回复“在避免风控的前提下进行尝试”。按限制范围、短时、不收发消息的单次实验执行，不承诺零风控风险。

## 实际执行

1. 复核固定 DLL 哈希、工具源码与已有只读预检。补充“控件树增长而触及遍历预算时，必须保留有限观测、不能丢失为泛化错误”的回归；先失败后修复。实验前基线仍要求完整且绑定稳定，未放宽写入准入。
2. 调用 `qt_accessibility_preflight.py --allow-temporary-byte-write` 一次。收据返回 error/ValueError，未生成 current_byte、before 或 trial 字段。按源码执行顺序，失败发生在 NativeByte 初始化中，未调用 trial 或 write；**本轮内存写入为 0**。该调用不能算完成了一次 0→1→0 实验。
3. 原收据没有细分 ValueError。为避免盲猜，补充仅允许自身固定错误码的记录逻辑，不记录任意异常文本。随后执行一次不带写入参数的只读预检，返回 target_not_unique。此结果是后续状态，不倒填为前一次异常的确定原因。
4. 继续只读核验窗口元数据：6 个 Weixin.exe 进程，符合主窗口类名／固定标题的 6 个窗口均 visible=false，可见主窗口为 0。未记录实际标题、聊天正文、账号或群标识，未激活窗口。

## 当前结论

**实验被前置条件拦下，未执行字节修改。** 后续核验明确发现当前窗口不可见，不能从隐藏的多个候选窗口中任选其一。没有自动重试写入，没有登录、收发、AI 调用、数据库或密钥访问。

此次只改变了工具的证据记录能力，不改变原窗口准入与字节写入条件。相关测试最终 44 项通过，包含截断观测保存、固定错误码输出及非白名单异常文本不泄露。测试不是实机写入／恢复成功证明。

下一步需要用户把当前账号微信主窗口打开并保持可见。已确认的单次实验范围继续有效，无需重新索取相同权限；继续时仍须重新预检，不根据旧句柄、截图或先前字节值直接写入。

收据：[带试验参数的写入前失败](pyweixin-qt-trial-blocked-2026-09-18.json)、[后续只读预检](pyweixin-qt-after-block-2026-09-18.json)。窗口布尔元数据留在本地研究目录 `window_diagnostic_20260918.json`；没有截图或个人内容进入仓库。
